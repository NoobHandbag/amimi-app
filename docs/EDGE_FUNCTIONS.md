# Amimì App - EDGE FUNCTIONS (riferimento)

> Una scheda per ogni edge function Supabase (project `imszbjeyplaiovylhkgl`). Creato 2026-07-06 dal codice in `supabase/functions/` + verifica live (`list_edge_functions`).
> Le VERSIONI cambiano a ogni deploy: quelle qui sotto sono lo stato al 2026-07-06 sera (round feedback cofounder); per la verita' live usare Supabase MCP `list_edge_functions`. Tutte le functions hanno `verify_jwt=false`.
> Deploy: SOLO via Supabase MCP `deploy_edge_function` (niente CLI link); e' territorio Claude Code (Regola Ferrea 16). I valori dei segreti NON vanno mai riportati nei doc (repo pubblico).

Pattern comuni: auth "PIN" = `body.pin` -> sha256 -> confronto con `app_config.pin_hash` (PIN neutralizzato a `x` per scelta owner: e' design, non sicurezza). Scritture con service-role key da env. Audit su `change_log`, telemetria su `health_log`.

## write-api (v20) - UNICO path di scrittura dati

- **Scopo:** ogni scrittura dati dell'ecosistema (app, Cowork, MCP) passa da qui.
- **Auth:** PIN.
- **Azioni:** `purchase`, `gift`, `b2b`, `product`, `order` (insert generici con validazione), `order_multi` (ordine fornitore multi-riga, un `gruppo`; dal v15 righe `wip:true` con qty 0 ammesse e stub prodotto con item/variant MAIUSCOLI), `arrival` (arrivo su ordine: aggiorna `qty_arrived` + inserisce in `purchases`), `arrival_set` (correzione qty_arrived con delta; dal v15 accetta `costo_unitario` opzionale e RISOLVE le righe WIP: ordinato = arrivato totale, wip=false), `count` (conta fisica: delta calcolato LATO SERVER da `v_inventory`, scrive `counts` + `stock_adjustments`), `product_verify` (completa anagrafica, accetta anche `cogs`; dal v15 item/variant forzati MAIUSCOLI lato server; dal v16 alla PRIMA verifica il CODICE si RIGENERA dai Modello+Variante finali di Benny — il codice dell'ordine di Ginni e' provvisorio — con rename a cascata su 8 tabelle e guardia anti-collisione), `expense_manual` / `expense_propose` / `expense_approve` (spese: dirette, in coda, approvazione con eventuale ricategorizzazione), `sale_correct` (riassegna una vendita Shopify/Qromo a un CODICE corretto, ri-snapshotta il COGS), `return` (reso/cambio; `sostituito_con` scala anche il sostituto; dal v15 `importo_rimborsato` e' CON SEGNO: negativo = il cliente paga la differenza), `qromo_sale` (forward idempotente, fallback COGS da `products`), `order_delete` (v15: elimina una riga ordine fornitore; 409 se ha arrivi registrati, salvo force), `reorder_archive` (v15: flag `products.riordino_archiviato` on/off), `product_delete` (brief 14-07, LIVE dal v20 19-07: elimina UNA riga di `products` con guardrail — 404 se assente; 409 se giacenza o conto vendita != 0, se ordini fornitore agganciati, o se riga in `shopify_stock` senza `force`; con movimenti storici serve `add_to_non_product:true` che inserisce il codice in `non_product_codici` cosi' i detector non si accendono e le vendite passate tengono il COGS snapshot; MAI tocca `product_aliases`; audit `change_log` op `product_delete` con before completo. Primo caso reale, doppione `LEA_BAG_DARK_ZEBRA`, eseguito il 19-07 con effetto identico via migr `0052`).
- **Protezioni:** blocco MESI CHIUSI (anno/mese presente in `ce_snapshots` -> 409 `closed_month`, si supera solo con `force` + motivo; ECCEZIONE v15: `expense_approve` con edit di sola nota su spesa gia' approved passa, il CE non si muove — era il bug 'le spese non si confermano'); validazioni input (CODICE senza spazi, quantita' > 0); idempotenza `qromo_sale` su `sale_id`; ogni op scrive `change_log` con `chi` e source.
- **Tocca:** legge `app_config`, `v_inventory`, `products`, `supplier_orders`, `expenses`; scrive `purchases`, `counts`, `stock_adjustments`, `products`, `expenses`, `returns`, `qromo_sales`, `shopify_line_items` (solo sale_correct), `gifts_offline`, `b2b_movements`, `supplier_orders`, `change_log`.

## shopify-sync (v5) - pull ordini Shopify

- **Scopo:** ingest SOLA LETTURA degli ordini Shopify nuovi + aggiornamento rimborsi/stato.
- **Auth:** PIN. Token Shopify da `app_config.shopify_token` (Admin API 2024-01).
- **Azioni:** default = pull ordini nuovi (idempotente su order_id) + re-sync `refund_amount`/`financial_status`/`fulfillment_status` degli ordini aggiornati negli ultimi 45gg (mai gli importi/righe); `backfill_meta` = aggiorna `fulfilled_at` + `discount_codes` + `customer_name` (dal v5, feedback 06-07 item 9; eseguito il 06-07: 273 ordini aggiornati) su righe esistenti; `dryRun` = preview senza insert.
- **Resolver (dal 06-07):** alias esatto -> `codice_norm` -> nome base senza suffisso " - Senza/Con Catena" -> SKU; una riga che non risolve entra con codice NULL e la intercetta il detector `shopify_orphan`.
- **Note:** payment fees ~2.2% + 0,25 EUR stimate sugli ordini live (i mesi storici da seed sono al centesimo); errori per-item raccolti e ritornati (max 5), mai silenzio.
- **Limite noto (06-07):** 179 ordini della replica (16-feb -> 07-mag) sono del VECCHIO store e non esistono sull'API dello store attuale: per quelli `customer_name` non e' recuperabile via Shopify (eventuale fonte: DB Shopify del Foglio Master legacy).

## shopify-stock (v12) - giacenze e push stock

- **Scopo:** specchio giacenze Shopify (`shopify_stock`) + push dello stock reale verso Shopify.
- **Auth:** PIN. Token Shopify da `app_config`.
- **Azioni:** `sync` (default, pull di tutti i prodotti/varianti: SKU -> codice via alias + codice_norm + fallback SKU; gestisce i dual-variant SC/CC = un codice, piu' `inventory_item_ids`; dal v10 salva anche `shopify_status` e quando PIU' prodotti Shopify mappano allo stesso codice titolo/immagine/status vengono dal migliore: attivo batte bozza, SKU esatto batte alias — fix immagine Savana/Leopardo, feedback 06-07 item 19); `realign_all` (autopush cron :27, gated `app_flags.shopify_autopush_enabled`; target = disponibili da vendere - buffer, oggi buffer 0; hold rialzi senza conta fresca <=30gg se `shopify_hold_raises='true'`; `dryRun` disponibile); `realign` (push manuale per codici scelti, gated `app_flags.shopify_write_enabled`, logga `chi`); `sync_now` (**dal v12**, brief 17-07: giro orario completo on-demand = `sync` -> `realign_all`, stesso codice dei cron via helper `doSync`/`doRealignAll`; il pulsante "Sincronizza Shopify adesso" nella tab Shopify dell'Inventario lo invoca con `chi`; audit `change_log` op `stock_sync_now`; **cooldown server-side 45s** anti doppio-click, superabile con `force:true`; se il pull Shopify fallisce il giro ABORTA senza riallineare su mirror stantio; flag sovrani invariati — con autopush spento il realign viene saltato e il client lo segnala).
- **Protezioni:** SKU non mappati MAI toccati (azzerarli nasconderebbe un prodotto live); push falliti VERI NON mascherati (ritornati come `failedCodici` + `health_log` chiave `stock_autopush` severity 'warn'); location da `app_flags.shopify_location_id`.
- **Untracked (dal v11):** un set fallisce anche quando l'`inventoryItem.tracked=false` (Shopify rifiuta la scrittura stock su un item non tracciato): NON e' un guasto, e' assenza di tracking magazzino. In `realign_all` un set fallito viene diagnosticato via GraphQL (`inventoryItem.tracked` + `variant.product.isGiftCard`, endpoint `2024-01/graphql.json`, stesso token): se untracked e non gift card finisce nel bucket **`untracked`** (non `failed`), riportato a parte nel summary/health_log e senza alzare la severity (niente warn perenne). Dietro flag `shopify_autoenable_tracking` (default off) l'autopush riaccende il tracking (`inventoryItemUpdate(tracked:true)`) e ritenta, **MAI su gift card**. `change_log` `stock_autopush` ora si scrive anche nei run con soli fallimenti/untracked. (Brief 08-07; caso reale `AGATA_BAG_PINK_CRYSTAL_BEADS` risolto a mano prima del deploy. Le 3 `GIFT CARD Amimì` restano `tracked:false` by design ma senza SKU non entrano in `shopify_stock`.)

## qromo-webhook (v5) - ricevitore vendite POS

- **Dal v5 (06-07):** se il payload Qromo porta un cliente (customer/client/customer_name), nome e cognome finiscono in `qromo_sales.nome/cognome`; le vendite POS restano di norma anonime.
- **Scopo:** riceve la vendita dalla console Qromo (webhook "Amimi App Supabase") e la scrive in `qromo_sales` (source `qromo-direct`). LIVE dal cutover 2026-07-03.
- **Auth (tripla, senza PIN):** `?key=` nell'URL, oppure `body.auth` = secret, oppure token Qromo in `app_flags.qromo_webhook_token`. Valori in `app_flags`, mai nei doc.
- **Comportamento:** POST only (GET risponde `online`); estrae l'ordine da body.order/data.order/payload.order; un record per item con paid=true; `sale_id` = order_id + indice item (stabile); prezzo = PAGATO per unita'; COGS snapshot da `products` se risolto, altrimenti `resolver_status='unresolved'` con nome raw in nota (mai perso).
- **Protezioni:** idempotenza via UNIQUE parziale `qromo_sales_live_saleid_uq` (23505 = re-delivery benigna, skip); errore vero su un item -> risposta non-200 cosi' Qromo RITENTA; item senza flag paid = skip segnalato.

## ce-guard (v3) - guardiano contabile

- **Scopo:** sorveglianza del CE e della qualita' dati; esiti in `health_log` (chiavi `ce_*`), letti dal banner rosso in Home.
- **Auth:** PIN. Cron **ORARIO** (`30 * * * *`, migr 0051; era daily 06:30 fino al 2026-07-09).
- **Notifiche ntfy (v3, 2026-07-09):** al termine del `run`, se cambia l'insieme delle CHIAVI dei problemi **error** (non i conteggi), pubblica una push sul topic ntfy del titolare (`https://ntfy.sh`, JSON, tag `warning`/`white_check_mark`, click alla app). "Solo su cambio" -> niente spam orario. Il topic vive in `app_flags.ntfy_topic` (service-role; se assente -> no-op); lo stato ultimo-notificato in `app_flags.ceguard_alert_state`. Le WARN (cogs mancanti, spese da verificare) NON notificano.
- **Azioni:** `run` (default) = 10 check: invarianti MC1/MC2 (tolleranza 0,02 EUR), `ce_qromo_unresolved`, `ce_cogs_mancanti` (warn), `ce_giacenze_negative`, `ce_expenses_categoria`, `ce_expenses_da_verificare` (warn), `ce_drift_mesi_chiusi` (da `v_ce_drift`, etichetta con netto E mc2), `ce_shopify_reconcile` (conteggio ordini API vs DB, mese corrente + precedente), `ce_shopify_token`, `ce_sync_freshness` (warn se `shopify_stock.synced_at` > 120 min). `close_month` (year, month, chi) = congela il CE del mese in `ce_snapshots` (UNIQUE, mai sovrascrive) + `change_log`. `status` = health_log `ce_*` di oggi.
- **Nota:** cancella e riscrive SOLO le chiavi `ce_*` di oggi (le altre sono di `refresh_health_log()`).

## ask-data (v4) - NL -> SQL

- **Scopo:** "Chiedi ai dati" del Cruscotto: Gemini (`gemini-flash-lite-latest`, temperature 0) genera una SELECT sullo schema esposto, eseguita da `ask_select` (SECURITY DEFINER: solo SELECT, singolo statement, max 200 righe, timeout 5s).
- **Auth:** PIN. Key Gemini in `app_flags.gemini_api_key` (se assente risponde `needs_key`).
- **Dal 06-07:** interroga `v_ce_totale` live (non piu' la copia storica).
- **APERTO (audit A1):** `ask_select` non ha allowlist di viste e ask-data e' pubblica: via di esfiltrazione segreti finche' l'owner non ruota i segreti e si chiude la falla.

## assistant (v8) - Assistente AI in-app (FLOW 6 v2, gated)

- **Scopo:** "Chiedi ad Amimì", pannello slide-in presente su OGNI schermata della PWA. Evoluzione di ask-data: risponde in italiano sui dati LIVE con testo + grafico + card prodotti con foto, e ricorda la conversazione. Design: `Cowork12/projects/Assistente_AI_2026-07/DESIGN_Assistente_AI_Amimi_V1.md` (Fase 1 di 3).
- **Pipeline (2 passi Gemini):** (1) `gemini-flash-lite-latest` traduce domanda + storia breve in UNA SELECT (schema-dal-reale CORRETTO: `categoria` = `BAG`/`ACCESSORY`/null, non piu' lo stale `ACCESSORI`/`PELLE`/`TESSUTO` di ask-data; glossario "borse tessili" = Nina/Agata/Annie via `item ILIKE`, netto = lordo/1,22); (2) `ask_select` esegue (SELECT-only, cap 200); (3) `gemini-flash-latest` in **JSON mode** (`responseMimeType: application/json`) scrive la prosa e MAPPA le colonne per grafico/prodotti. **I numeri arrivano SOLO da `ask_select`:** l'edge materializza barre e card DALLE righe reali (Gemini sceglie solo tipo/colonne), mai cifre inventate (Regola 1). Le card sono arricchite con foto/prezzo/giacenza reali da `v_inventory`.
- **Robustezza passo risposta (v6):** Flash a volte torna vuoto (il suo "thinking" esaurisce il budget token) e la risposta degradava a un generico "Ho trovato N risultati" senza grafico/card. Fix: se Flash torna vuoto/illeggibile, **retry automatico su `gemini-flash-lite-latest`** (non-thinking) prima del fallback generico. Stress test 8/9 prosa vera (il residuo era rate-limit free-tier sotto burst, non a carico reale). I numeri restano solo dalle righe.
- **How-to (Fase 2, v7):** il passo 1 (flash-lite) ora ROUTA: se la domanda chiede COME USARE l'app risponde con la sola parola `HOWTO`, altrimenti produce la SELECT. Sul ramo HOWTO l'assistente risponde dal **corpus how-to in `app_guides` (id=1)** — FAQ ancorate al codice reale dell'app (build via workflow `howto-corpus`, 25 sezioni, editabile senza redeploy via `corpus-load`) — e se la guida non copre la domanda indica la sezione dell'app, MAI passaggi inventati (Regola 1, design 4.4). La guardia read-only (imperativi azzera/cancella/...) e' esclusa per le frasi how-to ("come/dove/cosa vuol dire...") cosi' "come cancello un ordine?" arriva alla how-to invece del messaggio sola-lettura. Verificato live: "come registro un arrivo?" -> Ordini (non Registra, come diceva erroneamente il mockup).
- **Auth + gate:** `verify_jwt=false` + PIN (`x`), come ask-data. Gated da `app_config.ai_enabled` (default FALSE): a flag off risponde `{gated:true, testo:"L'assistente non e' attivo."}`. Key Gemini in `app_flags.gemini_api_key` (server-only).
- **Azioni (Fase 3, v8, gated `ai_actions_enabled` default OFF):** l'assistente NON scrive mai. Se `ai_actions_enabled` e una richiesta di REGISTRARE una spesa ("registra/aggiungi una spesa di 50€ per imballaggi"), l'edge la PARSA in una proposta `{azione:{tipo:'expense_propose', payload:{costo,categoria,operazione,amimi}, descrizione, categoria_valide}}` senza scrivere (mai un importo inventato: se manca il numero, niente proposta). L'utente rivede/aggiusta (soprattutto la categoria) e conferma nel frontend -> la scrittura passa da **write-api** `expense_propose` con `chi`, e la spesa resta **pending** (non tocca il CE finché non e' approvata a parte). A gate OFF, o per richieste non-spesa, torna il messaggio sola-lettura. Verificato: gate OFF -> nessuna proposta; propose -> 0 scritture (conteggi invariati); execute -> pending + change_log; test ripuliti, gate ri-OFF.
- **Input:** `{domanda, storia:[{ruolo,testo}], pin}`. **Output:** `{ok, gated?, testo, grafico?{tipo,titolo,etichette[],valori[]}, prodotti?[{codice,nome,variante,image_url,prezzo,disponibili,giacenza,stato,venduto_tot,valore,valore_label}], sql, righe}`.
- **Frontend:** componente globale `web/src/components/AssistantPanel.tsx` (FAB "Chiedi ad Amimì" + bottom-sheet, grafici inline SVG/CSS senza dipendenze, card con foto, blocco "fonti" con SQL e righe). Si auto-nasconde se `ai_enabled=false`, letto via `v_ops_flags.ai_enabled` (migr 0057). Il vecchio pannello inline "Chiedi ai dati" del Cruscotto (`Report.tsx`) e' stato rimosso perche' superato.
- **GO-LIVE (owner):** `app_config.ai_enabled='true'`.
- **Nota audit A1:** condivide con ask-data la via `ask_select` senza allowlist di viste -> stesso caveat esfiltrazione segreti finche' i segreti non sono ruotati/chiusi.

## corpus-load (v1) - loader della guida how-to (admin)

- **Scopo:** caricare/aggiornare il corpus how-to dell'assistente in `app_guides` (id=1). Esiste per POSTare da file via curl (il corpus e' ~21 KB) invece di incorporarlo/ribatterlo nel sorgente.
- **Auth:** PIN (`x`), `verify_jwt=false`. Scrive SOLO `app_guides` (knowledge base, non dati di business) con service-role; upsert `{content}`. Input `{pin, content}` -> `{ok, len}`.
- **Uso:** `curl -X POST .../corpus-load -d @payload.json` con `{pin:'x', content:<markdown>}`. Per aggiornare la guida basta ri-postare (nessun redeploy dell'assistant).

## mcp (v4) - server MCP per Claude

- **Scopo:** pilotare l'app da Claude (JSON-RPC 2.0, supporto SSE).
- **Auth:** tool read APERTI (`list_inventory`, `what_to_reorder`, `sku_availability`, `pnl_summary`, `ads_summary`, `ask_data`); tool write (`propose_expense`, `register_count`) dietro `Authorization: Bearer` = `app_flags.mcp_token`, delegati alla write-api con `chi='Claude-MCP'`.

## cs-sync (v3) - tool assistenza clienti, FASE 1 (ingest reale)

- **Scopo:** ingest della posta cliente di `info@amimi.it` nelle tabelle `cs_*` (design: `Cowork12/projects/Servizio_Clienti_2026-06/DESIGN_Tool_Assistenza_Amimi_V1_2026-07-20.md`). Fase 1 = coda SOLA LETTURA dietro login Supabase Auth; niente AI/bozze/invio (Fasi 2-4).
- **Auth:** PIN (`x`), `verify_jwt=false` (chiamabile dal cron). Chiave service account Google in `app_flags.cs_gmail_sa_key` (service-role only): OAuth 2.0 JWT grant RS256, impersona `info@amimi.it`, scope `gmail.readonly`.
- **Azione UNICA `poll`** `{pin, action:'poll', source?, dryRun?}`:
  - **Cursore** in `app_flags.cs_last_history_id`: primo giro (cursore vuoto) = SEED dal `historyId` del profilo (ingest NULLA, parte da qui in avanti, la coda non si riempie di storico); poi `history.list` incrementale; 404 (historyId scaduto) = RE-SEED + health_log warn.
  - **Classifica i 4 flussi cliente + pre-filtro rumore (100% deterministico, zero AI):** `email_diretta` (From = cliente, incl. risposte alle mail transazionali Shopify), `form_contatto`/`form_evento` (mittente Shopify CON reply-to del cliente), `chat_notifica` (`no-reply@mailer.shopify.com`, subject "New Message", read-only), `rumore` (notifica admin Shopify SENZA reply-to cliente, DMARC, bounce, newsletter Klaviyo, `@amimi.it`, + denylist estendibile `app_flags.cs_noise_senders`). Upsert idempotente (UNIQUE `gmail_thread_id`/`gmail_message_id`).
  - **Nessuna perdita di messaggi (corretto dopo review):** l'`historyId` di `history.list` e' il TIP della casella (identico su ogni pagina), non un cursore per-pagina; si avanza `cs_last_history_id` al tip SOLO se drenate tutte le pagine, altrimenti all'id dell'ULTIMO record interamente processato (backlog drenato a blocchi). Errore `messages.get`/DB transitorio (5xx/429/rete) = cursore FERMO, si riprova. Regola anti-perdita: mail non interpretabile entra COMUNQUE come conversazione grezza (`parse_failed=true`), mai persa, mai nel rumore.
  - `source='cron'` + `cs_enabled!='true'` -> skip: `cs_enabled` e' l'interruttore di go-live (owner). `dryRun=true` -> classifica i recenti e ritorna SOLO i conteggi per flusso, scrive nulla (smoke sicuro, niente contenuti esposti).
  - Telemetria: esito in `health_log` chiave `cs_sync` (severity ok/warn/error, n = parse_failed).
- **RIMOSSE le azioni Fase 0 `ping`/`status`** (esponevano subject/from dell'ultimo messaggio a chiunque avesse l'URL): **caveat sicurezza CHIUSO**. Verificato live: `ping`/`status` -> 422 `azione sconosciuta`.
- **Cron:** `cs-sync-poll` `*/2 * * * *` (migr 0054), no-op finche' `cs_enabled='false'`.
- **GO-LIVE (owner):** `app_flags.cs_enabled='true'` (GIA' true dal 2026-07-21, migr 0057). Da quel momento il cron ingerisce; una mail di test compare in coda entro ~2-5'. Tuning rumore senza redeploy: aggiungere mittenti/pattern (uno per riga o CSV) in `app_flags.cs_noise_senders`.
- **Denylist rumore caricata (2026-07-22, migr 0060 + 0063):** 36 voci in `app_flags.cs_noise_senders` (fonte `Cowork12/projects/Servizio_Clienti_2026-06/DENYLIST_cs_noise_senders.txt`). Semantica confermata su `isNoiseSender`: match SUBSTRING case-insensitive di ogni voce SIA su From(email) SIA su Subject; il pattern DMARC "Report Domain" e' anche hardcoded. Nessuna voce e' un dominio consumer (cross-check con `ALLOWLIST_domini_consumer.md`). Le 2 voci SENSIBILI `bancodesio.it`/`studiocssf.it` (banca/commercialista) caricate con OK owner esplicito (migr 0063): non sono clienti (fuori dalla coda CS) e restano fuori dal futuro classificatore Gemini (design 8.1); restano interamente in Gmail. Tuning senza redeploy: UNION idempotente sul flag. **Refresh 2026-07-22 (migr 0064): 36 -> 77 voci** (+41 mittenti-rumore dallo storico Takeout: corrieri, fintech, newsletter/mailing, fornitori materiali/servizi), stessa UNION idempotente esistenti-prima; verificato live 77/0 vuote/0 doppioni.
- **Frontend:** sezione "Assistenza" nella PWA (`web/`), client Supabase Auth DEDICATO (`lib/csClient.ts`, storageKey separato) cosi' il login non tocca il resto dell'app no-login; letture `cs_*` col JWT utente + RLS `authenticated`.
- **Hardening v3 (2a review avversariale):** (a) i byte NUL nel corpo si strippano a monte (`stripNull`) - Postgres text/jsonb li rifiuta e un solo messaggio velenoso avrebbe bloccato l'ingest (livelock); (b) `antiLoss` ora PROPAGA gli errori DB (prima li ingoiava -> una mail parse_failed poteva essere ne' salvata ne' ripresa = persa); (c) un giro fermato senza avanzamento (`stalled`) scrive `health_log` **warn**, non verde (uno stallo diventa visibile); (d) il re-processo non regredisce `last_msg_at`/ordine coda.

## cs-classify (v1) - tool assistenza clienti, FASE 2 (auto-categorizzazione + urgenza)

- **Scopo:** classifica le conversazioni cliente in UNA delle 13 categorie (tassonomia bloccata, design 6.2) e ne calcola l'urgenza. **DECOUPLED dall'ingest** (`cs-sync`): se Gemini e' giu' l'ingest continua e le card restano "da classificare"; il tool degrada senza bloccarsi.
- **Auth:** PIN (`x`), `verify_jwt=false` (chiamabile dal cron). Chiave Gemini in `app_flags.gemini_api_key` (server-only). service-role scrive `cs_conversations` + `cs_events`.
- **Modello:** `gemini-flash-lite-latest`, temperature 0, JSON mode (`responseMimeType`, come `assistant`; MAI `thinkingConfig`). Output vincolato `{categoria, categoria_confidence, lingua, urgente, urgenza_motivo, flags}`.
- **Azione `classify`** (default) `{pin, action:'classify', source?, dryRun?, limit?}`: pesca fino a `MAX_PER_RUN=10` conversazioni con `categoria IS NULL`, `canale != 'rumore'`, `parse_failed=false` (piu' vecchie prima); per ognuna manda a Gemini `subject + ultimo messaggio in + form fields` e ci sovrappone le **regole deterministiche di urgenza** (codice, non AI): 2+ messaggi `in` senza risposta = `sollecito`; parole disputa/chargeback/banca = `reclamo_assistenza` + urgente; cliente in loco ("sono qua fuori/al portone") = urgente; thread `fatto` che riapre = `sollecito`. `urgenza_motivo` SEMPRE valorizzato quando urgente. **Sotto soglia** (`categoria_confidence < 0.6` o categoria vuota) -> `categoria` resta NULL con `categoria_source='ai_low'` ("da confermare", mai una categoria inventata; Regola 1 portata nell'AI). Un errore Gemini su una conversazione la salta (resta da classificare), non blocca le altre. `dryRun=true` -> anteprima, nessuna scrittura. Scrive `cs_events azione='classify'`.
- **Azione `classify_text`** `{pin, action:'classify_text', text}`: classifica un TESTO grezzo e ritorna il JSON AI, **senza scrivere**. Serve al benchmark (golden set) per misurare l'ESATTA logica AI di produzione. Solo lo strato AI (categoria + urgenza AI): le regole deterministiche dipendono dai messaggi e non si applicano qui.
- **Cron:** `cs-classify` `*/5 * * * *` (migr 0066), no-op se `cs_enabled!='true'`. Decoupled dal `cs-sync-poll` `*/2`.
- **Benchmark golden set (criterio d'accettazione):** classificatore girato via `classify_text` sulle 203 conversazioni scoreable di `EMAIL_classificate_2026-07-20.csv` (251 righe meno 48 tra RUMORE e FLAG). **Ultimo aggregato PULITO (v2, 0 errori): 77,3%** (v1 74,9% -> v2 77,3% dopo iterazione principiata del prompt). **v3 deployato** (fix regressione Restock, spot-verificato) atteso ~79-80%, ma un aggregato v3 pulito e' **bloccato dalla quota giornaliera Gemini free** (esaurita in sessione dopo 2 run completi; reset ~24h) -> da rimisurare. **>=80% NON dimostrato sull'aggregato grezzo**, MA il gap e' l'INPUT del golden set, non il classificatore: la colonna `nota` e' una glossa di workflow (nostro handling), non il testo cliente; ~15/46 disaccordi hanno `nota` senza contenuto-cliente classificabile (in produzione il classificatore vede testo reale + form_fields + canale). Sulle righe con contenuto la qualita' e' ~85%; forzare l'aggregato con few-shot iper-specifici = overfitting (vietato dal brief). **Decisione owner** sul criterio. Mappatura dichiarata: `Collaborazioni B2B spam`->📢; `Acquisto assistito`->💬 Altro; `Reclamo assistenza`/`Problema canale`/`Chiusura ringraziamento`->FLAG (esclusi dallo score); `Notifica amministrativa`/`Test interno`->RUMORE (esclusi). Report completo: `Cowork12/projects/Servizio_Clienti_2026-06/BENCHMARK_classificatore_2026-07-22.md`.

## cs-api (v1) - tool assistenza clienti, FASE 2 (scritture dalla UI, JWT-gated)

- **Scopo:** prima scrittura dalla UI (correzione manuale categoria). E' il pattern che servira' anche alla Fase 4 per gli stati.
- **Auth (belt-and-suspenders):** `verify_jwt=false` (l'auth la fa la funzione); l'`Authorization: Bearer <access_token>` deve essere di un UTENTE Supabase Auth reale (verificato con `getUser`: la anon key NON e' un utente -> rifiutata) E l'email deve finire in `@amimi.it`. Solo allora un client service-role scrive. La UI NON scrive mai diretto (nessuna policy RLS di scrittura). Test negativi verificati live: no header -> 401 `non autenticato`; anon key -> 401 `sessione non valida`; token invalido -> 401. Nessuna scrittura nei casi negativi.
- **Azione `set_categoria`** `{action:'set_categoria', conversation_id, categoria, chi}`: valida `categoria` sulla tassonomia (o `''`/null -> torna "da confermare"), scrive `categoria_source='manuale'` + `cs_events azione='categoria_edit'` con `chi` = identita' del selettore (Benny/Ginni/Ale, design 3.4) e `by_email` nell'audit. Stati/bozze/invio = Fasi 3-4 (fuori scope).

## cs-assist (v2) - tool assistenza clienti, FASE 3 (recupero dati + riassunto/storia + bozza)

- **Scopo:** genera il RIASSUNTO+storia cliente e la BOZZA di risposta con dati reali. Il recupero dati e' **DETERMINISTICO** (dal codice, non dall'AI); Gemini scrive SOLO usando quel blocco, con `[DA VERIFICARE: ...]` dove un dato manca (Regola Ferrea 1 portata nell'AI). **Nessun invio** (Fase 4).
- **Recupero DATI (deterministico, condiviso):** prodotti citati -> match `v_inventory` (modello+variante, fuzzy) + fallback `product_aliases` (strict, ~70% parole) -> giacenza/disponibili/prezzo/on_shopify; ordine -> `shopify_orders` per `order_number` (da `cs_conversations.order_number`) o `customer_email` + `shopify_line_items` (contenuto); tracking fresco best-effort via Shopify Admin API (`orders.json?name=%23<n>&fields=id,name,fulfillments`, perche' il DB tiene `order_id='#1518'` come NOME, non l'id numerico); tono/risposte standard da `cs_faq`.
- **Azione `dry_data` (PIN):** assembla il blocco DATI + `fonti[]` per una conversazione, NESSUN Gemini, NESSUNA scrittura. Serve a testare il recupero dati e a mostrare le "Fonti" in UI. Verificato live su ordine reale #1518 + prodotto AGATA PAILLETTES PINK (disponibili/prezzo corretti, righe ordine, alias-noise eliminato in v2).
- **Azione `summary` (PIN, cron `*/7`, migr 0067):** riempie `cs_conversations.summary`/`summary_at` dove NULL (canale != rumore). Riassunto 2 righe che incrocia la storia per `customer_email` (altre conversazioni dello stesso cliente). Gemini `flash-lite`. Decoupled da ingest/classify; NO-OP se `cs_enabled!='true'`. `cs_events azione='summary'`.
- **Azione `draft` (JWT):** on-demand dalla UI. Gate come `cs-api` (`getUser` + `@amimi.it`; anon/no-token -> 401 verificati). Assembla DATI -> Gemini `flash` (retry flash-lite) -> bozza; STILE nel prompt (dai del tu, frasi corte, emoji leggere, firma "Grazie, Team Amimi'", niente promesse su date fuori dai DATI). Scrive `cs_drafts` (testo, `dati_usati` jsonb, model) + `cs_events azione='draft'` con `chi` = selettore. Ritorna `{draft, fonti, da_verificare}`.
- **Frontend:** thread view mostra il riassunto (card) + pulsante "Genera bozza con i dati reali" -> textarea editabile + badge "N da verificare" + card **Fonti** + **Copia** (Fase 3 = copia nel thread Gmail; l'invio dal tool e' Fase 4).
- **cs_faq seedata (migr 0067):** 6 `esempio_tono` (skeleton di STILE con placeholder, nessun dato/fatto inventato). Le `faq`/`risposta_standard` con contenuti REALI le aggiunge l'owner (cs_faq editabile): non si inventano fatti di brand.
- **VERIFICA (criterio design: 7/10 bozze inviabili con ritocchi minimi, zero numeri inventati):** la parte deterministica e' verificata live; la generazione Gemini (summary/draft) attende il reset quota Gemini free (esaurita in giornata dai benchmark Fase 2) + giudizio umano Benny/Ginni. Codice sui pattern gia' provati (cs-classify/cs-api).
- **v3 (2026-07-23): libreria risposte + guardrail escalation.** (a) Seed `cs_faq` con 12 `risposta_standard` IT/EN (migr 0069, spec Cowork PARTE A) coi valori operativi confermati dall'owner (reso 14gg, corriere TWS, codice PERTE, ritiro Via Plinio 43 provvisorio); `faqTono` le inietta filtrando `categoria === conv.categoria` -> le `categoria` DEVONO essere le 13 stringhe ESATTE del classificatore (senza emoji), verificato via `dry_data` (A1 iniettata su una conv Spedizione). (b) Prompt `draft` esteso coi criteri PARTE B3: casi da NON chiudere da sola (difetto/garanzia -> mai negare il reso citando i 14gg, garanzia legale 24 mesi; disputa/chargeback/banca; reclamo/rivenditore/B2B/preventivo -> escalation), + "dai del lei se il cliente e' arrabbiato". Deploy verificato (dry_data ok post-deploy). La qualita' delle bozze (7/10) resta da misurare a quota Gemini ripristinata (summary/draft ancora in errore quota il 23-07).

## loyalty-proxy (v2) - punti fedelta' via App Proxy (identita' Shopify)

- **Scopo:** slice loyalty. Un cliente **loggato su Shopify** guadagna punti (gioco "Amimì Click") salvati in Supabase, senza secondo login. Dimostra il canale read+write sicuro via identita' Shopify (App Proxy). Sottosistema NON-core, ADDITIVO, gated.
- **Auth (HMAC App Proxy, NON JWT):** `verify_jwt=false`. Shopify firma le richieste proxate col **client secret** dell'app; la edge ricostruisce l'HMAC-SHA256 dei query param (ordinati per chiave, coppie `key=value` concatenate SENZA separatore, esclusa `signature`), in hex, confronto timing-safe. Firma assente/errata -> 401, nessuna scrittura. Distinto dall'HMAC base64 dei webhook Shopify.
- **Identita':** SOLO `logged_in_customer_id` FIRMATO dai params, MAI dal body. Vuoto (cliente non loggato) -> `{state:'login_required'}`, nessuna scrittura.
- **Segreto:** `app_flags.shopify_app_proxy_secret` (convenzione codebase, come qromo_webhook_secret/gemini_api_key), fallback env `SHOPIFY_APP_PROXY_SECRET`. Assente -> `{state:'needs_secret'}` 200 (deployabile/testabile PRIMA della config Shopify, non crash). L'owner/Cowork lo inserisce DOPO aver configurato l'app (canale sicuro, mai in repo/chat).
- **Azioni** (dal path App Proxy `/apps/premia/<azione>` o `?action=`): `balance` (GET) -> `{points}` del cliente (0 se assente); `add` (POST `{score}`) -> anti-abuso PRIMA di scrivere, poi `loyalty_points.points += clamp` + insert `loyalty_events`.
- **Anti-abuso:** score clampato a `CAP_PER_GAME=100`; cap giornaliero `DAILY_CAP=200` punti/cliente (somma dei delta di oggi UTC); rate-limit `RATE_LIMIT_SEC=30` (1 partita/30s). Oltre soglia -> `{capped:true, reason:'rate'|'daily'|'zero'}` SENZA sforare ne' scrivere. `capped` nella risposta = accreditato MENO del richiesto (clamp-partita o cap giornaliero).
- **Scritture:** SOLO `loyalty_points` + `loyalty_events` col service_role (RLS nega ogni accesso client, SCHEMA §7). NON passa da write-api: come `cs_*`, la edge protetta E' il canale di scrittura controllato (spirito Regola 13; non tocca CE/stock/inventario/Qromo).
- **Verifica (2026-07-23, criteri brief 2-7):** firma assente/manomessa -> 401 no-write; empty customer -> login_required; add normale (50), clamp 150->100, rate-limit, cap giornaliero tutti corretti; `loyalty_events` coerente (0 scritture sui path cappati, persistenza verificata); anon/authenticated 0 privilegi SELECT/INSERT (RLS+REVOKE); needs_secret senza segreto; 0 segreti nel bundle. Testato con secret di prova TEMPORANEO (migr `tmp_loyalty_smoke_0068` + `_cleanup`, tabelle poi vuote, edge tornata a needs_secret). **Criterio 1 (firma valida end-to-end) = con la config Shopify reale, owner/Cowork.**
- **URL edge (target dell'App Proxy):** `https://imszbjeyplaiovylhkgl.supabase.co/functions/v1/loyalty-proxy`. Config lato Shopify (app + App Proxy prefix `apps`, subpath `premia`, URL=questo) + client secret = Cowork/owner, fuori dal repo.
- **RESTA (owner/Cowork):** configurare l'app Shopify + App Proxy, mettere il client secret in `app_flags.shopify_app_proxy_secret`, montare il clicker in una sezione nascosta e chiudere il criterio 1. Fase dopo (non questa slice): sconto personale reale via Admin API.

## etl-load (v4) - RITIRATA

- Stub che risponde 410 Gone: era il loader one-off del re-seed 2026-07-01. Nessuna azione. Candidata alla cancellazione.

## Flag e config di riferimento

- `app_config`: `pin_hash`, `shopify_token`, `iva_rate` (0.22), `parity_tolerance_cents`, `ai_enabled` (gate dell'assistente AI in-app, default false, go-live owner, dal 2026-07-22; esposto ad anon come booleano via `v_ops_flags`, migr 0057).
- `app_flags`: `shopify_write_enabled`, `shopify_autopush_enabled`, `shopify_expose_buffer` (oggi 0), `shopify_hold_raises`, `shopify_autoenable_tracking` (default off: riaccende il tracking magazzino su item fisici untracked prima del set, mai gift card, dal v11), `shopify_location_id`, `qromo_webhook_secret`, `qromo_webhook_token`, `gemini_api_key`, `mcp_token`. Entrambe le tabelle sono service-role only (lockdown migr 0026); i flag si cambiano SOLO con decisione owner (Regola 15/16).
- Chiavi `cs_*` (tool assistenza, dal 2026-07-20, Fase 0): `cs_enabled` ('false' = sync spento), `cs_last_history_id` (cursore Gmail, vuoto), `cs_gmail_sa_key` (chiave JSON service account, vuota finche' l'owner non la inserisce via canale sicuro), `cs_ntfy_topic_benny` / `cs_ntfy_topic_ginevra` / `cs_ntfy_topic_ale` (topic ntfy con suffisso random generato direttamente nel DB: i valori NON sono mai transitati in repo, doc o chat e sono da trattare come segreti). Inserite via SQL one-off, NON via migrazione (repo pubblico).
