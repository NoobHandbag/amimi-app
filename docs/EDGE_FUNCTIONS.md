# Amimì App - EDGE FUNCTIONS (riferimento)

> Una scheda per ogni edge function Supabase (project `imszbjeyplaiovylhkgl`). Creato 2026-07-06 dal codice in `supabase/functions/` + verifica live (`list_edge_functions`).
> Le VERSIONI cambiano a ogni deploy: quelle qui sotto sono lo stato al 2026-07-06 sera (round feedback cofounder); per la verita' live usare Supabase MCP `list_edge_functions`. Tutte le functions hanno `verify_jwt=false`.
> Deploy: SOLO via Supabase MCP `deploy_edge_function` (niente CLI link); e' territorio Claude Code (Regola Ferrea 16). I valori dei segreti NON vanno mai riportati nei doc (repo pubblico).

Pattern comuni: auth "PIN" = `body.pin` -> sha256 -> confronto con `app_config.pin_hash` (PIN neutralizzato a `x` per scelta owner: e' design, non sicurezza). Scritture con service-role key da env. Audit su `change_log`, telemetria su `health_log`.

## write-api (v23) - UNICO path di scrittura dati

- **Scopo:** ogni scrittura dati dell'ecosistema (app, Cowork, MCP) passa da qui.
- **Auth:** PIN.
- **Azioni:** `purchase`, `gift`, `b2b`, `product` (insert generici con validazione; **`order` DISABILITATO dal v21**: bypassava derivazione e anti-doppione, nel client era dead code -> 400 con puntatore a order_multi), `order_multi` (ordine fornitore multi-riga, un `gruppo`; dal v15 righe `wip:true` con qty 0 ammesse; **dal v21 il CODICE e' "generated, never typed"**, brief campi_necessari_prodotto A: per ogni riga (1) se il codice del client norm-matcha un prodotto -> RIORDINO col codice canonico del DB, mai ri-derivato; (2) altrimenti con Modello+Variante il codice e' DERIVATO server-side con la `tok` v2 condivisa — NFD anti-accenti, ogni sequenza non alfanumerica -> `_` — e ri-matchato per norm, cosi' un riordino scritto "quasi uguale" NON crea stub; (3) altrimenti 422; coerenza gruppo: stesso codice con varianti diverse -> 422, era l'origine del caso Rita; gli STUB si scrivono PRIMA delle righe ordine con errore CONTROLLATO e upsert su `codice_norm` — se l'anagrafica fallisce l'ordine NON viene scritto, niente stub ingoiati; lo stub NON scrive piu' `status` ne' `categoria` fissa 'BAG': la categoria la da' `models`), `arrival` (arrivo su ordine: aggiorna `qty_arrived` + inserisce in `purchases`), `arrival_set` (correzione qty_arrived con delta; dal v15 accetta `costo_unitario` opzionale e RISOLVE le righe WIP), `count` (conta fisica: delta calcolato LATO SERVER da `v_inventory`), `product_verify` (**dal v21 la completezza E' il segnale**, brief B: bloccanti model+variant+`retail_price>0`+`cogs>0` sulla TRANSIZIONE a verificato — con `confirm:true` dal pannello un salvataggio incompleto torna 422 con `mancanti[]`; senza confirm e' un edit di campo che passa sempre (il COGS-editor del catalogo non si rompe) ma `verificato` scatta solo ad anagrafica completa e NON retrocede mai (timbro audit); il RENAME al codice definitivo usa i Modello+Variante FINALI, dal DB se il payload non li porta, finche' il prodotto non e' mai stato verificato o il codice e' provvisorio; guardie v21: niente rename se il codice ha righe in `shopify_stock` (qualsiasi status: la cascata non tocca il mirror, Regola 15) e il rename da-DB vale solo per gli stub `app-ordine` (sui legacy serve item+variant espliciti nel payload); collisione: 409 se provvisorio con messaggio operativo che sfrutta il reap, altrimenti salvataggio senza rinomina + warning; cascata rename su 10 tabelle), `expense_manual` / `expense_propose` / `expense_approve`, `sale_correct`, `return`, `qromo_sale` (forward idempotente, fallback COGS), `order_delete` (v15: 409 se ha arrivi; v17: reap dello stub app-ordine mai toccato all'ultima riga), `reorder_archive`, `product_delete` (v20, guardrail invariati), `expenses_bulk` (v22: carico mensile spese, dry_run default, validazione per riga, dedup segnalato, force_closed_month per riga), **`row_delete` (v23, brief 31-07: cancellazione VERA di righe, decisione owner dopo il doppio conteggio arrivi)**: whitelist tabelle (`purchases`, `supplier_orders`, `gifts_offline`, `returns`, `stock_adjustments`, `qromo_sales`, `b2b_movements`, `expenses`; FUORI products/shopify_*/change_log/health_log/ce_snapshots/app_*), max 20 id per chiamata (Regola 2), `motivo` obbligatorio, **before-image COMPLETA in change_log PRIMA della delete** (`op='row_delete'`, `before`=riga intera+motivo, `after`=null: ogni cancellazione e' ricostruibile), guardia mesi chiusi da subito (year/month della riga o data/data_ordine/date_paid; 409 senza `force`), `dry_run` DEFAULT true (si cancella solo con `false` esplicito), segnalazione `orphan_purchases` quando si cancellano righe `supplier_orders` (match codice+fornitore+source `app-arrivo%`; la data non entra nel match di proposito: gli arrivi parziali cadono su date diverse).
- **Protezioni:** blocco MESI CHIUSI (`ce_snapshots` -> 409 `closed_month`, si supera con `force`; eccezione nota-only su expense_approve); validazioni input; idempotenza `qromo_sale` su `sale_id`; ogni op scrive `change_log` con `chi` e source.
- **Tocca:** legge `app_config`, `v_inventory`, `products`, `supplier_orders`, `expenses`, `shopify_stock` (guardia rename v21); scrive `purchases`, `counts`, `stock_adjustments`, `products`, `expenses`, `returns`, `qromo_sales`, `shopify_line_items` (solo sale_correct), `gifts_offline`, `b2b_movements`, `supplier_orders`, `change_log`.
- **Smoke v21 (23/24-07, dati test ripuliti):** riordino con codice scritto male -> nessuno stub; derivato con trattino/accento UTF-8 -> match; verify confirm incompleta -> 422 `mancanti`; edit COGS senza confirm su prodotto con buchi -> 200; verify completa senza item nel payload -> rename da DB + cascata su supplier_orders; `order` -> 400; reap su order_delete OK.

### expenses_bulk (v22, 2026-07-29) - carico mensile spese da xlsx

Nasce per chiudere l'ultimo pezzo della migrazione finanza: il task `expenses-master-upload-mensile` scriveva le spese sul Foglio Master via webapp Fix Anagrafica, cosa vietata dalla Regola Ferrea 12. Dal 29-07 il task e' declassato a solo-xlsx (produce `EXPENSES_prep_YYYY-MM.xlsx` con le righe normalizzate) e le righe entrano da questa azione.

- **Input:** `{action:'expenses_bulk', pin, chi, dry_run, rows:[...]}`. `rows` sta al livello ALTO del body (accettato anche dentro `payload`). Ogni riga: `data` (obbligatoria, **solo ISO `YYYY-MM-DD`**), `descrizione`, `costo`, `categoria`, `amimi`, opzionali `sottocategoria`, `note`, `fonte`, `force_closed_month`.
- **Output:** `{ok, dry_run, totali:{ricevute, valide, respinte, duplicati, inserite}, righe:[{i, esito, descrizione, motivo?, duplicato?, id?}]}`. `esito` e' `valida` (dry-run), `inserita` (apply) o `respinta` col `motivo` in chiaro. `i` e' l'indice nella `rows` di partenza, cosi' l'operatore sa QUALE riga dell'xlsx sistemare.
- **`dry_run` e' il DEFAULT.** Si scrive solo con `dry_run` uguale a `false` booleano o alla stringa `"false"`; qualunque altro valore (`0`, `"no"`, assente) resta dry-run. Su un carico che muove il CE il default deve sbagliare verso il non-scrivere (Regola 2).
- **Validazione PER RIGA, mai tutto-o-niente:** le righe buone passano, le cattive tornano col motivo. L'INSERT delle valide e' invece UNA sola chiamata: se fallisce a livello DB non resta mezzo carico dentro. Una riga di `change_log` (op `expenses_bulk`, con `chi`) per ogni spesa inserita.
- **8 categorie, non 7:** `COGS, LOGISTICA, MARKETING, OPEX, PACKAGING, SALARI, TASSE, EVENTI`. Sono quelle della colonna generata `expenses.categoria_valid`, che ammette anche EVENTI (categoria vera: 1 spesa a catalogo e riga `eventi` in entrambi i CE). Il brief ne elencava 7: escludere EVENTI avrebbe respinto spese legittime. Come sempre la generata non si scrive, si valida l'input (Regola 14).
- **Data solo ISO, di proposito:** `new Date('07/08/2026')` e' l'8 luglio per il parser e il 7 agosto per un italiano. Su dati contabili quell'ambiguita' sposta una spesa di mese in silenzio, quindi il formato non ISO si RESPINGE invece di indovinare. Respinte anche `15/07/2026` e `2026-7-5`.
- **Descrizione obbligatoria:** una spesa senza descrizione non e' verificabile nel CE ed e' invisibile al dedup.
- **Costo:** sempre normalizzato a negativo (`-Math.abs`), come `expense_manual`; `0` e i non-numeri sono respinti.
- **Dedup SEGNALATO, non bloccante:** stessa `date_paid` + stesso importo (tolleranza 0,005) + descrizione normalizzata (minuscole, accenti piegati, non-alfanumerici a spazio) uguale o contenuta l'una nell'altra. Regola volutamente prevedibile, nessuna soglia fuzzy: la riga passa con `duplicato: <id esistente>` e decide l'operatore.
- **Guardia mesi chiusi:** data in un mese presente in `ce_snapshots` -> respinta, con override `force_closed_month: true` **per riga**. Non esiste un force globale: forzare va deciso una spesa alla volta (Regola 11).
- **Cap 200 righe** per payload (422 con messaggio esplicito); `rows` vuoto -> 422.
- **Letture:** una sola per tutto il carico (`ce_snapshots` e `expenses`), non una per riga.
- **Verifica (29-07):** 40/40 su test a fixture. Live in dry-run: payload misto -> 5 esiti distinti e 0 scritture (279 spese invariate, 0 righe `change_log`); il dedup ha agganciato la spesa vera "Spazio Plinio" del 10-07 partendo da `SPAZIO  PLINIO` con doppio spazio; 201 righe -> 422. Apply provato con 2 righe da 0,01 EUR (autorizzazione owner in chat): inserite solo le valide, 1 riga di `change_log` a testa, comparse nel mese giusto dei due CE (`opex` luglio da -400,00 a -400,01), `categoria_valid`/`amimi` calcolate dal DB, mese chiuso inserito solo con force. Righe di test poi **rimosse su autorizzazione esplicita dell'owner** (deroga puntuale alla Regola 13, che vieta i DELETE diretti): ripristinati 279 spese, 0 residui, `v_ce_drift` a 0, marzo e luglio ai valori di partenza.
- **Handback a Cowork:** ripuntare il prompt del task a "dry_run via `expenses_bulk` + apply su conferma owner", riga in `_CHANGELOG.md`, chiudere la proposta P5 in `_SYNC_REPORT.md`.

## shopify-sync (v5) - pull ordini Shopify

- **Scopo:** ingest SOLA LETTURA degli ordini Shopify nuovi + aggiornamento rimborsi/stato.
- **Auth:** PIN. Token Shopify da `app_config.shopify_token` (Admin API 2024-01).
- **Azioni:** default = pull ordini nuovi (idempotente su order_id) + re-sync `refund_amount`/`financial_status`/`fulfillment_status` degli ordini aggiornati negli ultimi 45gg (mai gli importi/righe); `backfill_meta` = aggiorna `fulfilled_at` + `discount_codes` + `customer_name` (dal v5, feedback 06-07 item 9; eseguito il 06-07: 273 ordini aggiornati) su righe esistenti; `dryRun` = preview senza insert.
- **Resolver (dal 06-07):** alias esatto -> `codice_norm` -> nome base senza suffisso " - Senza/Con Catena" -> SKU; una riga che non risolve entra con codice NULL e la intercetta il detector `shopify_orphan`.
- **Note:** payment fees ~2.2% + 0,25 EUR stimate sugli ordini live (i mesi storici da seed sono al centesimo); errori per-item raccolti e ritornati (max 5), mai silenzio.
- **Limite noto (06-07):** 179 ordini della replica (16-feb -> 07-mag) sono del VECCHIO store e non esistono sull'API dello store attuale: per quelli `customer_name` non e' recuperabile via Shopify (eventuale fonte: DB Shopify del Foglio Master legacy).

## shopify-stock (v13) - giacenze e push stock

- **Scopo:** specchio giacenze Shopify (`shopify_stock`) + push dello stock reale verso Shopify.
- **Auth:** PIN. Token Shopify da `app_config`.
- **Azioni:** `sync` (default, pull di tutti i prodotti/varianti: SKU -> codice via alias + codice_norm + fallback SKU; gestisce i dual-variant SC/CC = un codice, piu' `inventory_item_ids`; dal v10 salva anche `shopify_status` e quando PIU' prodotti Shopify mappano allo stesso codice titolo/immagine/status vengono dal migliore: attivo batte bozza, SKU esatto batte alias — fix immagine Savana/Leopardo, feedback 06-07 item 19); `realign_all` (autopush cron :27, gated `app_flags.shopify_autopush_enabled`; target = disponibili da vendere - buffer, oggi buffer 0; hold rialzi senza conta fresca <=30gg se `shopify_hold_raises='true'`; `dryRun` disponibile); `realign` (push manuale per codici scelti, gated `app_flags.shopify_write_enabled`, logga `chi`); `sync_now` (**dal v12**, brief 17-07: giro orario completo on-demand = `sync` -> `realign_all`, stesso codice dei cron via helper `doSync`/`doRealignAll`; il pulsante "Sincronizza Shopify adesso" nella tab Shopify dell'Inventario lo invoca con `chi`; audit `change_log` op `stock_sync_now`; **cooldown server-side 45s** anti doppio-click, superabile con `force:true`; se il pull Shopify fallisce il giro ABORTA senza riallineare su mirror stantio; flag sovrani invariati — con autopush spento il realign viene saltato e il client lo segnala).
- **Paginazione + prune (dal v13, brief 23-07):** il pull `sync` segue `rel="next"` nell'header `Link` di `products.json` (250 prodotti per pagina): prima era una SINGOLA fetch e oltre i 250 prodotti gli eccedenti non venivano mai sincronizzati, in silenzio (mirror gia' a 245 codici al 23-07). Una pagina fallita aborta l'intero giro (nessun upsert/prune su pull parziale); cap 20 pagine con errore esplicito, mai troncamento silenzioso. A valle di un pull completo e non vuoto, le righe di `shopify_stock` non piu' viste (prodotto eliminato da Shopify) vengono RIMOSSE: prima restavano per sempre con `synced_at` congelato e, se active, tenevano `on_shopify=true` in `v_inventory` (caso `ANNIE_BAG_SILK_BROWN`, 23 righe ferme al 06-07, ripulite col primo sync v13). Audit: `change_log` op `stock_prune` (`chi` = cron o utente del `sync_now`, codici rimossi max 40 in `after`); la risposta di `sync` include anche `pruned` e `pages`. Il prune tocca SOLO la tabella mirror, mai Shopify: Regola Ferrea 15 e gate invariati.
- **Protezioni:** SKU non mappati MAI toccati (azzerarli nasconderebbe un prodotto live); push falliti VERI NON mascherati (ritornati come `failedCodici` + `health_log` chiave `stock_autopush` severity 'warn'); location da `app_flags.shopify_location_id`.
- **Untracked (dal v11):** un set fallisce anche quando l'`inventoryItem.tracked=false` (Shopify rifiuta la scrittura stock su un item non tracciato): NON e' un guasto, e' assenza di tracking magazzino. In `realign_all` un set fallito viene diagnosticato via GraphQL (`inventoryItem.tracked` + `variant.product.isGiftCard`, endpoint `2024-01/graphql.json`, stesso token): se untracked e non gift card finisce nel bucket **`untracked`** (non `failed`), riportato a parte nel summary/health_log e senza alzare la severity (niente warn perenne). Dietro flag `shopify_autoenable_tracking` (default off) l'autopush riaccende il tracking (`inventoryItemUpdate(tracked:true)`) e ritenta, **MAI su gift card**. `change_log` `stock_autopush` ora si scrive anche nei run con soli fallimenti/untracked. (Brief 08-07; caso reale `AGATA_BAG_PINK_CRYSTAL_BEADS` risolto a mano prima del deploy. Le 3 `GIFT CARD Amimì` restano `tracked:false` by design ma senza SKU non entrano in `shopify_stock`.)

## qromo-webhook (v5) - ricevitore vendite POS

- **Dal v5 (06-07):** se il payload Qromo porta un cliente (customer/client/customer_name), nome e cognome finiscono in `qromo_sales.nome/cognome`; le vendite POS restano di norma anonime.
- **Scopo:** riceve la vendita dalla console Qromo (webhook "Amimi App Supabase") e la scrive in `qromo_sales` (source `qromo-direct`). LIVE dal cutover 2026-07-03.
- **Auth (tripla, senza PIN):** `?key=` nell'URL, oppure `body.auth` = secret, oppure token Qromo in `app_flags.qromo_webhook_token`. Valori in `app_flags`, mai nei doc.
- **Comportamento:** POST only (GET risponde `online`); estrae l'ordine da body.order/data.order/payload.order; un record per item con paid=true; `sale_id` = order_id + indice item (stabile); prezzo = PAGATO per unita'; COGS snapshot da `products` se risolto, altrimenti `resolver_status='unresolved'` con nome raw in nota (mai perso).
- **Protezioni:** idempotenza via UNIQUE parziale `qromo_sales_live_saleid_uq` (23505 = re-delivery benigna, skip); errore vero su un item -> risposta non-200 cosi' Qromo RITENTA; item senza flag paid = skip segnalato.

## ce-guard (v4) - guardiano contabile

- **Scopo:** sorveglianza del CE e della qualita' dati; esiti in `health_log` (chiavi `ce_*`), letti dal banner rosso in Home.
- **Auth:** PIN. Cron **ORARIO** (`30 * * * *`, migr 0051; era daily 06:30 fino al 2026-07-09).
- **Notifiche ntfy (v3, 2026-07-09):** al termine del `run`, se cambia l'insieme delle CHIAVI dei problemi **error** (non i conteggi), pubblica una push sul topic ntfy del titolare (`https://ntfy.sh`, JSON, tag `warning`/`white_check_mark`, click alla app). "Solo su cambio" -> niente spam orario. Il topic vive in `app_flags.ntfy_topic` (service-role; se assente -> no-op); lo stato ultimo-notificato in `app_flags.ceguard_alert_state`. Le WARN (cogs mancanti, spese da verificare) NON notificano.
- **Azioni:** `run` (default) = 13 check: invarianti MC1/MC2 (tolleranza 0,02 EUR), `ce_qromo_unresolved`, `ce_cogs_mancanti` (warn), `ce_giacenze_negative`, `ce_expenses_categoria`, `ce_expenses_da_verificare` (warn), `ce_drift_mesi_chiusi` (da `v_ce_drift`, etichetta con netto E mc2), `ce_shopify_reconcile` (conteggio ordini API vs DB, mese corrente + precedente), `ce_shopify_token`, `ce_sync_freshness` (warn se `shopify_stock.synced_at` > 120 min), piu' le 3 sentinelle del collegamento app <-> Shopify qui sotto. `close_month` (year, month, chi) = congela il CE del mese in `ce_snapshots` (UNIQUE, mai sovrascrive) + `change_log`. `status` = health_log `ce_*` di oggi.
- **Nota:** cancella e riscrive SOLO le chiavi `ce_*` di oggi (le altre sono di `refresh_health_log()`).

### Collegamento app <-> Shopify: 3 sentinelle (v4, 2026-07-29)

Nate dal caso **LEA BAG BLACK & WHITE PONY**: scheda ACTIVE con SKU `Lea_Bag_MUCCATA_NERA`, cioe' un codice diverso ma ancora a catalogo (doppione storico, giacenza 0). In `shopify-stock` la mappatura variante -> codice va **prima per SKU** e usa l'alias del TITOLO solo se lo SKU non matcha nessun codice: con uno SKU che matcha un codice vero l'alias non entra mai in gioco, l'autopush spinge la disponibilita' del codice fantasma (0) e il codice vero non ha nemmeno una riga in `shopify_stock`. Guasto silenzioso da aprile a fine luglio: non e' `unmapped`, il push riesce (Shopify accetta lo 0), le vendite arrivano corrette dal resolver per NOME di `shopify-sync`. Nessun controllo lo vedeva.

Tutte e tre sono in **sola lettura**: segnalano, non riparano. Correggere uno SKU in automatico su un mismatch mal interpretato sposterebbe lo stock del prodotto sbagliato, quindi la correzione resta un gesto umano (owner, o Cowork con OK owner).

| Chiave | Cosa vede | Severity | Cosa fare quando scatta |
|---|---|---|---|
| `ce_sku_mismatch` | Scheda il cui SKU aggancia un codice diverso da quello suggerito dall'alias sul titolo. `n` = le sole schede **active**; il totale (draft comprese) e' nell'etichetta con 2 esempi. | `warn` se almeno una active, altrimenti `ok` (una draft non e' vendibile e non deve tenere acceso un warn perenne, come il bucket `untracked` di `shopify-stock`) | Aprire la scheda su Shopify e decidere quale codice e' quello giusto: di norma vince l'alias sul titolo (e' quello su cui risolvono le vendite). Corretto lo SKU, il check rientra al giro dopo. |
| `ce_sku_unmapped_active` | Scheda **active** il cui codice risolto non esiste a catalogo: `realign_all` la mette nel bucket `unmapped` e la salta, quindi quel prodotto **non riceve mai stock**. | `warn` se > 0 | Mettere sullo SKU il CODICE_AMIIMI vero, oppure aggiungere il prodotto a catalogo se manca davvero. Draft e archived restano fuori per scelta (~88 residui del vecchio catalogo, rumore). |
| `ce_stock_senza_scheda` | Codici con `disponibili_da_vendere > 0` e nessuna riga in `shopify_stock`: merce in magazzino che il sito non puo' vendere. | **sempre `ok`**, informativo (spesso e' una scelta di catalogo, un warn qui sarebbe perenne). Unico check dove `n > 0` con severity `ok` e' voluto: e' un contatore. | Se il prodotto va venduto online, pubblicarlo. Se e' voluto che resti fuori, aggiungere il codice all'allowlist `app_flags.ceguard_no_shopify_ignore` (codici separati da virgola, tollera spazi e minuscole; se il flag non esiste il check gira lo stesso). |

Dettagli di implementazione da non perdere:

- **La normalizzazione del titolo deve restare identica a quella di `shopify-stock`.** Le due righe gemelle sono `const norm = (s) => (s ? s.toUpperCase().replace(/\s+/g, '_') : '')` in `shopify-stock/index.ts` (riga 8) e la stessa riga nel blocco 10 di `ce-guard/index.ts`. Se una cambia, cambiano entrambe: altrimenti il check diventa cieco proprio sui titoli con spazi doppi.
- **Join in JS con una Map, non in SQL.** `product_aliases` ha `shopify_name_norm` duplicati (14 al 29-07): un join SQL moltiplicherebbe le righe e gonfierebbe i conteggi.
- **Confronti case-insensitive** (upper + spazi a underscore su entrambi i lati): gli SKU legacy in `Title_Case` sono validi per scelta (Regola Ferrea 4, i join passano da `codice_norm`), quindi una differenza di sole maiuscole non e' un mismatch.
- **`ce_sku_unmapped_active` guarda il codice RISOLTO, non lo SKU grezzo** (`shopify_stock` non conserva lo SKU). Uno SKU sbagliato ma recuperato dall'alias del titolo finisce sul codice giusto, lo stock va dove deve e per scelta non compare qui: la sentinella elenca solo le schede che non ricevono stock.
- **Copertura di `ce_sku_mismatch`:** sulle schede active il cui titolo non ha alcun alias il check non puo' pronunciarsi. Quante sono e' scritto in coda all'etichetta (6 al 29-07).
- **Le chiavi iniziano per `ce_` e devono continuare a farlo.** Il cron e' ORARIO: la delete di fine run filtra `k like 'ce_%'`, quindi una chiave senza prefisso sopravviverebbe, al secondo giro del giorno sbatterebbe sull'unique index `health_log(day,k)` e farebbe fallire l'INSERT intero, spegnendo la guardia per il resto della giornata. In piu' `action=status`, il banner rosso in Home e `refresh_health_log()` (migr 0035) filtrano tutti sul prefisso.
- **Nessuna notifica ntfy:** le tre sentinelle sono `warn`/`ok` e la firma delle push guarda solo le chiavi `error`. Si vedono nella pagina Salute, non nel banner rosso.

Valori al 2026-07-29 (deploy v4): `ce_sku_mismatch` 1 scheda 0 active (ANNIE BAG PINK SILK, draft: SKU `ANNIE_BAG_SILK_PINK` vs titolo `ANNIE_BAG_PINK`) -> ok; `ce_sku_unmapped_active` 0 (ANNIE BAG BROWN SILK e' stata corretta dall'owner a SKU `ANNIE_BAG_BROWN` nel pomeriggio, verificato via Admin API) -> ok; `ce_stock_senza_scheda` 8 codici / 10 pezzi (3 LEA_BAG_TOASTED_LEATHER_GREEN, 5 PORTA_CARTE_COCCO, AGATA_BAG_FLOWER_LILLA, LEA_BAG_CAVALLINO_CHOCOLATE) -> informativo.

## ask-data (v4) - NL -> SQL

- **Scopo:** "Chiedi ai dati" del Cruscotto: Gemini (`gemini-flash-lite-latest`, temperature 0) genera una SELECT sullo schema esposto, eseguita da `ask_select` (SECURITY DEFINER: solo SELECT, singolo statement, max 200 righe, timeout 5s).
- **Auth:** PIN. Key Gemini in `app_flags.gemini_api_key` (se assente risponde `needs_key`).
- **Dal 06-07:** interroga `v_ce_totale` live (non piu' la copia storica).
- **APERTO (audit A1):** `ask_select` non ha allowlist di viste e ask-data e' pubblica: via di esfiltrazione segreti finche' l'owner non ruota i segreti e si chiude la falla.

## assistant (v8) - Assistente AI in-app (FLOW 6 v2, gated)

- **Scopo:** "Chiedi ad Amimì", pannello slide-in presente su OGNI schermata della PWA. Evoluzione di ask-data: risponde in italiano sui dati LIVE con testo + grafico + card prodotti con foto, e ricorda la conversazione. Design: `Cowork12/projects/Assistente_AI_2026-07/DESIGN_Assistente_AI_Amimi_V1.md` (Fase 1 di 3).
- **Pipeline (2 passi Gemini):** (1) `gemini-flash-lite-latest` traduce domanda + storia breve in UNA SELECT (schema-dal-reale CORRETTO: `categoria` = `BAG`/`ACCESSORY`/null, non piu' lo stale `ACCESSORI`/`PELLE`/`TESSUTO` di ask-data; glossario "borse tessili" = Nina/Agata/Annie via `item ILIKE`, netto = lordo/1,22); (2) `ask_select` esegue (SELECT-only, cap 200); (3) `gemini-flash-latest` in **JSON mode** (`responseMimeType: application/json`) scrive la prosa e MAPPA le colonne per grafico/prodotti. **I numeri arrivano SOLO da `ask_select`:** l'edge materializza barre e card DALLE righe reali (Gemini sceglie solo tipo/colonne), mai cifre inventate (Regola 1). Le card sono arricchite con foto/prezzo/giacenza reali da `v_inventory`.
- **Robustezza passo risposta (v6):** Flash a volte torna vuoto (il suo "thinking" esaurisce il budget token) e la risposta degradava a un generico "Ho trovato N risultati" senza grafico/card. Fix: se Flash torna vuoto/illeggibile, **retry automatico su `gemini-flash-lite-latest`** (non-thinking) prima del fallback generico. Stress test 8/9 prosa vera (il residuo era rate-limit free-tier sotto burst, non a carico reale). I numeri restano solo dalle righe.
- **How-to (Fase 2, v7):** il passo 1 (flash-lite) ora ROUTA: se la domanda chiede COME USARE l'app risponde con la sola parola `HOWTO`, altrimenti produce la SELECT. Sul ramo HOWTO l'assistente risponde dal **corpus how-to in `app_guides` (id=1)** — FAQ ancorate al codice reale dell'app (build via workflow `howto-corpus`, 25 sezioni, editabile senza redeploy via `corpus-load`) — e se la guida non copre la domanda indica la sezione dell'app, MAI passaggi inventati (Regola 1, design 4.4). La guardia read-only (imperativi azzera/cancella/...) e' esclusa per le frasi how-to ("come/dove/cosa vuol dire...") cosi' "come cancello un ordine?" arriva alla how-to invece del messaggio sola-lettura. Verificato live: "come registro un arrivo?" -> Ordini (non Registra, come diceva erroneamente il mockup).
- **Auth + gate:** `verify_jwt=false` + PIN (`x`), come ask-data. Gated da `app_config.ai_enabled` (default FALSE): a flag off risponde `{gated:true, testo:"L'assistente non e' attivo."}`. Key Gemini in `app_flags.gemini_api_key` (server-only).
- **Azioni (Fase 3, v8, gated `ai_actions_enabled` default OFF):** l'assistente NON scrive mai. Se `ai_actions_enabled` e una richiesta di REGISTRARE una spesa ("registra/aggiungi una spesa di 50€ per imballaggi"), l'edge la PARSA in una proposta `{azione:{tipo:'expense_propose', payload:{costo,categoria,operazione,amimi}, descrizione, categoria_valide}}` senza scrivere (mai un importo inventato: se manca il numero, niente proposta). L'utente rivede/aggiusta (soprattutto la categoria) e conferma nel frontend -> la scrittura passa da **write-api** `expense_propose` con `chi`, e la spesa resta **pending** (non tocca il CE finché non e' approvata a parte). A gate OFF, o per richieste non-spesa, torna il messaggio sola-lettura. Verificato: gate OFF -> nessuna proposta; propose -> 0 scritture (conteggi invariati); execute -> pending + change_log; test ripuliti, gate ri-OFF.
- **Input:** `{domanda, storia:[{ruolo,testo}], pin}`. **Output:** `{ok, gated?, testo, grafico?{tipo,titolo,etichette[],valori[]}, prodotti?[{codice,nome,variante,image_url,prezzo,disponibili,giacenza,stato,venduto_tot,valore,valore_label}], sql, righe}`.
- **Frontend:** componente globale `web/src/components/AssistantPanel.tsx` (FAB "Chiedi ad Amimì" + bottom-sheet, grafici inline SVG/CSS senza dipendenze, card con foto, blocco "fonti" con SQL e righe). Si auto-nasconde se `ai_enabled=false`, letto via `v_ops_flags.ai_enabled` (migr 0057). Il vecchio pannello inline "Chiedi ai dati" del Cruscotto (`Report.tsx`) e' stato rimosso perche' superato.
- **GO-LIVE (owner):** `app_config.ai_enabled='true'`.
- **Nota audit A1:** condivide con ask-data la via `ask_select` senza allowlist di viste -> stesso caveat esfiltrazione segreti finche' i segreti non sono ruotati/chiusi.

## corpus-load (v1) - loader della guida how-to (admin)

- **Scopo:** caricare/aggiornare il corpus how-to dell'assistente in `app_guides` (id=1). Esiste per POSTare da file via curl (il corpus e' ~21 KB) invece di incorporarlo/ribatterlo nel sorgente.
- **Auth:** PIN (`x`), `verify_jwt=false`. Scrive SOLO `app_guides` (knowledge base, non dati di business) con service-role; upsert `{content}`. Input `{pin, content}` -> `{ok, len}`.
- **Uso:** `curl -X POST .../corpus-load -d @payload.json` con `{pin:'x', content:<markdown>}`. Per aggiornare la guida basta ri-postare (nessun redeploy dell'assistant).

## mcp (v6) - server MCP per Claude

- **Scopo:** pilotare l'app da Claude (JSON-RPC 2.0, supporto SSE).
- **Auth:** tool read APERTI (`list_inventory`, `what_to_reorder`, `sku_availability`, `pnl_summary`, `ads_summary`, `ask_data`); tool write (`propose_expense`, `register_count`, `delete_rows`) dietro `Authorization: Bearer` = `app_flags.mcp_token`, delegati alla write-api con `chi='Claude-MCP'`.
- **`delete_rows` (v6, brief 31-07):** wrapper del `row_delete` di write-api con guardia aggiuntiva: il dry-run gira SEMPRE per primo; per cancellare davvero servono `dry_run:false` E `conferma` = numero di righe visto nel dry-run (mismatch -> rifiuto con isError). Cosi' un agente non cancella 200 righe credendo di cancellarne 2. `force` passa al body write-api (mesi chiusi). Collaudato end-to-end il 31-07 (dry, conferma sbagliata rifiutata, delete vera con before-image `chi='Claude-MCP'`).
- **Nota repo (31-07):** il file nel repo era rimasto alla v3 (mancava il fix 'cerca' in-query del 06-07 presente nella v4 live): riallineato alla v4 PRIMA di aggiungere delete_rows, da qui in poi il repo e' la fonte.

## cs-sync (v5) - tool assistenza clienti, FASE 1 (ingest reale)

- **Scopo:** ingest della posta cliente di `info@amimi.it` nelle tabelle `cs_*` (design: `Cowork12/projects/Servizio_Clienti_2026-06/DESIGN_Tool_Assistenza_Amimi_V1_2026-07-20.md`). Fase 1 = coda SOLA LETTURA dietro login Supabase Auth; niente AI/bozze/invio (Fasi 2-4).
- **Auth:** PIN (`x`), `verify_jwt=false` (chiamabile dal cron). Chiave service account Google in `app_flags.cs_gmail_sa_key` (service-role only): OAuth 2.0 JWT grant RS256, impersona `info@amimi.it`, scope `gmail.readonly`.
- **Azione UNICA `poll`** `{pin, action:'poll', source?, dryRun?}`:
  - **Cursore** in `app_flags.cs_last_history_id`: primo giro (cursore vuoto) = SEED dal `historyId` del profilo (ingest NULLA, parte da qui in avanti, la coda non si riempie di storico); poi `history.list` incrementale; 404 (historyId scaduto) = RE-SEED + health_log warn.
  - **Classifica i 4 flussi cliente + pre-filtro rumore (100% deterministico, zero AI):** `email_diretta` (From = cliente, incl. risposte alle mail transazionali Shopify), `form_contatto`/`form_evento` (mittente Shopify CON reply-to del cliente), `chat_notifica` (`no-reply@mailer.shopify.com`, subject "New Message", read-only), `rumore` (notifica admin Shopify SENZA reply-to cliente, DMARC, bounce, newsletter Klaviyo, `@amimi.it`, + denylist estendibile `app_flags.cs_noise_senders`). Upsert idempotente (UNIQUE `gmail_thread_id`/`gmail_message_id`).
  - **Nessuna perdita di messaggi (corretto dopo review):** l'`historyId` di `history.list` e' il TIP della casella (identico su ogni pagina), non un cursore per-pagina; si avanza `cs_last_history_id` al tip SOLO se drenate tutte le pagine, altrimenti all'id dell'ULTIMO record interamente processato (backlog drenato a blocchi). Errore `messages.get`/DB transitorio (5xx/429/rete) = cursore FERMO, si riprova. Regola anti-perdita: mail non interpretabile entra COMUNQUE come conversazione grezza (`parse_failed=true`), mai persa, mai nel rumore.
  - `source='cron'` + `cs_enabled!='true'` -> skip: `cs_enabled` e' l'interruttore di go-live (owner). `dryRun=true` -> classifica i recenti e ritorna SOLO i conteggi per flusso, scrive nulla (smoke sicuro, niente contenuti esposti).
  - Telemetria: esito in `health_log` chiave `cs_sync` (severity ok/warn/error, n = parse_failed).
- **RIMOSSE le azioni Fase 0 `ping`/`status`** (esponevano subject/from dell'ultimo messaggio a chiunque avesse l'URL): **caveat sicurezza CHIUSO**. Verificato live: `ping`/`status` -> 422 `azione sconosciuta`.
- **Cron:** `cs-sync-poll` `*/2 * * * *` (migr 0054), no-op finche' `cs_enabled='false'`.
- **GO-LIVE (owner):** `app_flags.cs_enabled='true'` (GIA' true dal 2026-07-21, migr 0057). Da quel momento il cron ingerisce; una mail di test compare in coda entro ~2-5'. Tuning rumore senza redeploy: aggiungere mittenti/pattern (uno per riga o CSV) in `app_flags.cs_noise_senders`.
- **Denylist rumore caricata (2026-07-22, migr 0060 + 0063):** 36 voci in `app_flags.cs_noise_senders` (fonte `Cowork12/projects/Servizio_Clienti_2026-06/DENYLIST_cs_noise_senders.txt`). Semantica confermata su `isNoiseSender`: match SUBSTRING case-insensitive di ogni voce SIA su From(email) SIA su Subject; il pattern DMARC "Report Domain" e' anche hardcoded. Nessuna voce e' un dominio consumer (cross-check con `ALLOWLIST_domini_consumer.md`). Le 2 voci SENSIBILI `bancodesio.it`/`studiocssf.it` (banca/commercialista) caricate con OK owner esplicito (migr 0063): non sono clienti (fuori dalla coda CS) e restano fuori dal futuro classificatore Gemini (design 8.1); restano interamente in Gmail. Tuning senza redeploy: UNION idempotente sul flag. **Refresh 2026-07-22 (migr 0064): 36 -> 77 voci** (+41 mittenti-rumore dallo storico Takeout: corrieri, fintech, newsletter/mailing, fornitori materiali/servizi), stessa UNION idempotente esistenti-prima; verificato live 77/0 vuote/0 doppioni.
- **Frontend:** sezione "Assistenza" nella PWA (`web/`), client Supabase Auth DEDICATO (`lib/csClient.ts`, storageKey separato) cosi' il login non tocca il resto dell'app no-login; letture `cs_*` col JWT utente + RLS `authenticated`.
- **v5 (2026-07-26): email del cliente dalla chat.** Nelle notifiche `chat_notifica`, se il "nome" nel subject ("New message from X") e' un indirizzo email (il visitatore l'ha lasciata in chat), va in `customer_email` (lowercase) -> sblocca storia acquisti, aggancio ordine e bozze anche per la chat. Backfill storico: migr 0079 (nel DB: name 0078_cs_chat_email_backfill). La RISPOSTA resta dentro Shopify Inbox (Inbox non ha API pubblica, verificato 26-07): la UI ora estrae il deep-link "Reply in Inbox" dal corpo della notifica e apre la conversazione esatta.
- **Hardening v3 (2a review avversariale):** (a) i byte NUL nel corpo si strippano a monte (`stripNull`) - Postgres text/jsonb li rifiuta e un solo messaggio velenoso avrebbe bloccato l'ingest (livelock); (b) `antiLoss` ora PROPAGA gli errori DB (prima li ingoiava -> una mail parse_failed poteva essere ne' salvata ne' ripresa = persa); (c) un giro fermato senza avanzamento (`stalled`) scrive `health_log` **warn**, non verde (uno stallo diventa visibile); (d) il re-processo non regredisce `last_msg_at`/ordine coda.

## cs-classify (v6) - auto-categorizzazione + urgenza (14 categorie)

- **Scopo:** classifica le conversazioni cliente in UNA delle categorie (design 6.2) e ne calcola l'urgenza. **DECOUPLED dall'ingest** (`cs-sync`): se Gemini e' giu' l'ingest continua e le card restano "da classificare"; il tool degrada senza bloccarsi.
- **v6 (2026-07-24, DEPLOYATA): fix audit.** (1) pesca solo le MAI TENTATE (`categoria IS NULL AND categoria_source IS NULL`): le ai_low non si riclassificano piu' all'infinito (niente loop quota Gemini/starvation; si correggono a mano via cs-api); (2) update con guard `.is('categoria', null)`: una correzione manuale arrivata mentre Gemini girava NON viene sovrascritta; (3) `dryRun` preview senza `id`/`urgenza_motivo` (niente leak UUID/PII dietro PIN pubblico). Smoke: dryRun -> `{classified:0, remaining:0}` (coda mai-tentate vuota, query nuova ok).
- **v5 (2026-07-23): 14a categoria "Modifica / correzione indirizzo"** (era 13). Aggiunta con OK owner: colma il gap noto della ricerca (indirizzi errati/incompleti finivano in Cambio/Spedizione/Altro). CANON + prompt (definizione + few-shot) aggiornati; `toCanon` la accetta (match esatto, parole non in conflitto con le altre). Aggancia la risposta `cs_faq` A13. NB: il benchmark 77,3% era su 13 categorie -> da ri-misurare a quota Gemini ripristinata. Le conversazioni gia' classificate NON si ri-classificano (solo `categoria IS NULL`): gli indirizzi vecchi restano nella categoria precedente.
- **Auth:** PIN (`x`), `verify_jwt=false` (chiamabile dal cron). Chiave Gemini in `app_flags.gemini_api_key` (server-only). service-role scrive `cs_conversations` + `cs_events`.
- **Modello:** `gemini-flash-lite-latest`, temperature 0, JSON mode (`responseMimeType`, come `assistant`; MAI `thinkingConfig`). Output vincolato `{categoria, categoria_confidence, lingua, urgente, urgenza_motivo, flags}`.
- **Azione `classify`** (default) `{pin, action:'classify', source?, dryRun?, limit?}`: pesca fino a `MAX_PER_RUN=10` conversazioni con `categoria IS NULL`, `canale != 'rumore'`, `parse_failed=false` (piu' vecchie prima); per ognuna manda a Gemini `subject + ultimo messaggio in + form fields` e ci sovrappone le **regole deterministiche di urgenza** (codice, non AI): 2+ messaggi `in` senza risposta = `sollecito`; parole disputa/chargeback/banca = `reclamo_assistenza` + urgente; cliente in loco ("sono qua fuori/al portone") = urgente; thread `fatto` che riapre = `sollecito`. `urgenza_motivo` SEMPRE valorizzato quando urgente. **Sotto soglia** (`categoria_confidence < 0.6` o categoria vuota) -> `categoria` resta NULL con `categoria_source='ai_low'` ("da confermare", mai una categoria inventata; Regola 1 portata nell'AI). Un errore Gemini su una conversazione la salta (resta da classificare), non blocca le altre. `dryRun=true` -> anteprima, nessuna scrittura. Scrive `cs_events azione='classify'`.
- **Azione `classify_text`** `{pin, action:'classify_text', text}`: classifica un TESTO grezzo e ritorna il JSON AI, **senza scrivere**. Serve al benchmark (golden set) per misurare l'ESATTA logica AI di produzione. Solo lo strato AI (categoria + urgenza AI): le regole deterministiche dipendono dai messaggi e non si applicano qui.
- **Cron:** `cs-classify` `*/5 * * * *` (migr 0066), no-op se `cs_enabled!='true'`. Decoupled dal `cs-sync-poll` `*/2`.
- **Benchmark golden set (criterio d'accettazione):** classificatore girato via `classify_text` sulle 203 conversazioni scoreable di `EMAIL_classificate_2026-07-20.csv` (251 righe meno 48 tra RUMORE e FLAG). **Ultimo aggregato PULITO (v2, 0 errori): 77,3%** (v1 74,9% -> v2 77,3% dopo iterazione principiata del prompt). **v3 deployato** (fix regressione Restock, spot-verificato) atteso ~79-80%, ma un aggregato v3 pulito e' **bloccato dalla quota giornaliera Gemini free** (esaurita in sessione dopo 2 run completi; reset ~24h) -> da rimisurare. **>=80% NON dimostrato sull'aggregato grezzo**, MA il gap e' l'INPUT del golden set, non il classificatore: la colonna `nota` e' una glossa di workflow (nostro handling), non il testo cliente; ~15/46 disaccordi hanno `nota` senza contenuto-cliente classificabile (in produzione il classificatore vede testo reale + form_fields + canale). Sulle righe con contenuto la qualita' e' ~85%; forzare l'aggregato con few-shot iper-specifici = overfitting (vietato dal brief). **Decisione owner** sul criterio. Mappatura dichiarata: `Collaborazioni B2B spam`->📢; `Acquisto assistito`->💬 Altro; `Reclamo assistenza`/`Problema canale`/`Chiusura ringraziamento`->FLAG (esclusi dallo score); `Notifica amministrativa`/`Test interno`->RUMORE (esclusi). Report completo: `Cowork12/projects/Servizio_Clienti_2026-06/BENCHMARK_classificatore_2026-07-22.md`.

## cs-api (v3) - scritture UI (categoria, stato coda, non-e-cliente, config AI)

- **Scopo:** prima scrittura dalla UI (correzione manuale categoria). E' il pattern che servira' anche alla Fase 4 per gli stati.
- **Auth (belt-and-suspenders):** `verify_jwt=false` (l'auth la fa la funzione); l'`Authorization: Bearer <access_token>` deve essere di un UTENTE Supabase Auth reale (verificato con `getUser`: la anon key NON e' un utente -> rifiutata) E l'email deve finire in `@amimi.it`. Solo allora un client service-role scrive. La UI NON scrive mai diretto (nessuna policy RLS di scrittura). Test negativi verificati live: no header -> 401 `non autenticato`; anon key -> 401 `sessione non valida`; token invalido -> 401. Nessuna scrittura nei casi negativi.
- **Azione `set_categoria`** `{action:'set_categoria', conversation_id, categoria, chi}`: valida `categoria` sulla tassonomia (o `''`/null -> torna "da confermare"), scrive `categoria_source='manuale'` + `cs_events azione='categoria_edit'` con `chi` = identita' del selettore (Benny/Ginni/Ale, design 3.4) e `by_email` nell'audit. Stati/bozze/invio = Fasi 3-4 (fuori scope).

## cs-assist (v10) - contesto + 3 opzioni + verdetti + motore AI + LINTER DI ADERENZA

- **v10 (31-07, brief harness eval):** `model_override` opzionale su `draft`/`refine` (SOLO rami JWT), allowlist esplicita `['gemini-flash-latest','claude-sonnet-5']` (fuori lista -> 400; su altre azioni -> 400); chiave del motore richiesto assente -> `needs_key`, MAI fallback silenzioso sull'altro motore (falserebbe l'A/B; con override anche il retry Gemini flash-lite e' disattivato); `cs_drafts.model` riflette SEMPRE il modello reale. `source:'eval'` nel body marca la bozza in `cs_drafts.source` (migr 0080) e nel dettaglio di `cs_events`: righe di eval distinguibili con `select * from cs_drafts where source='eval'`; la UI non le mostra mai (legge solo la risposta live, mai la tabella). Harness CLI: `scripts/cs-eval.mjs` (auth SOLO access token utente @amimi.it via env `CS_EVAL_TOKEN`, sequenziale con pausa, stop su 429, output .json completo + .md anonimizzato). Senza override il comportamento e' IDENTICO al v9. NOTA runLLM/summary invariata: se Claude e' configurato senza override, gli errori Claude si propagano (502, nessun fallback a Gemini) e `summary` usa comunque Gemini.
- **Scopo:** genera il RIASSUNTO+storia cliente e la BOZZA di risposta con dati reali. Il recupero dati e' **DETERMINISTICO** (dal codice, non dall'AI); Gemini scrive SOLO usando quel blocco, con `[DA VERIFICARE: ...]` dove un dato manca (Regola Ferrea 1 portata nell'AI). **Nessun invio** (Fase 4).
- **Recupero DATI (deterministico, condiviso):** prodotti citati -> match `v_inventory` (modello+variante, fuzzy) + fallback `product_aliases` (strict, ~70% parole) -> giacenza/disponibili/prezzo/on_shopify; ordine -> `shopify_orders` per `order_number` (da `cs_conversations.order_number`) o `customer_email` + `shopify_line_items` (contenuto); tracking fresco best-effort via Shopify Admin API (`orders.json?name=%23<n>&fields=id,name,fulfillments`, perche' il DB tiene `order_id='#1518'` come NOME, non l'id numerico); tono/risposte standard da `cs_faq`.
- **Azione `dry_data` (PIN):** assembla il blocco DATI + `fonti[]` per una conversazione, NESSUN Gemini, NESSUNA scrittura. Serve a testare il recupero dati e a mostrare le "Fonti" in UI. Verificato live su ordine reale #1518 + prodotto AGATA PAILLETTES PINK (disponibili/prezzo corretti, righe ordine, alias-noise eliminato in v2).
- **Azione `summary` (PIN, cron `*/7`, migr 0067):** riempie `cs_conversations.summary`/`summary_at` dove NULL (canale != rumore). Riassunto 2 righe che incrocia la storia per `customer_email` (altre conversazioni dello stesso cliente). Gemini `flash-lite`. Decoupled da ingest/classify; NO-OP se `cs_enabled!='true'`. `cs_events azione='summary'`.
- **Azione `draft` (JWT):** on-demand dalla UI. Gate come `cs-api` (`getUser` + `@amimi.it`; anon/no-token -> 401 verificati). Assembla DATI -> Gemini `flash` (retry flash-lite) -> bozza; STILE nel prompt (dai del tu, frasi corte, emoji leggere, firma "Grazie, Team Amimi'", niente promesse su date fuori dai DATI). Scrive `cs_drafts` (testo, `dati_usati` jsonb, model) + `cs_events azione='draft'` con `chi` = selettore. Ritorna `{draft, fonti, da_verificare}`.
- **Frontend:** thread view mostra il riassunto (card) + pulsante "Genera bozza con i dati reali" -> textarea editabile + badge "N da verificare" + card **Fonti** + **Copia** (Fase 3 = copia nel thread Gmail; l'invio dal tool e' Fase 4).
- **cs_faq seedata (migr 0067):** 6 `esempio_tono` (skeleton di STILE con placeholder, nessun dato/fatto inventato). Le `faq`/`risposta_standard` con contenuti REALI le aggiunge l'owner (cs_faq editabile): non si inventano fatti di brand.
- **VERIFICA (criterio design: 7/10 bozze inviabili con ritocchi minimi, zero numeri inventati):** la parte deterministica e' verificata live; la generazione Gemini (summary/draft) attende il reset quota Gemini free (esaurita in giornata dai benchmark Fase 2) + giudizio umano Benny/Ginni. Codice sui pattern gia' provati (cs-classify/cs-api).
- **v9 (2026-07-26, DEPLOYATA): bozze anche per la chat del sito.** `chat_notifica` non e' piu' esclusa da context/draft/refine: (a) `stripChat` toglie il boilerplate della notifica Shopify Inbox ("You have a new message from ... / Sent via Inbox / Reply in Inbox (url)") dal testo passato al modello e al match prodotti; (b) prompt draft/refine in TONO CHAT (niente oggetto/formato email) quando `canale='chat_notifica'`; (c) il fallback a bozza singola usa un replace regex che copre entrambe le varianti (email/chat) della frase di sistema. La UI sblocca contesto+bozze per le chat e il bottone "Rispondi in Inbox" apre la conversazione esatta (deep-link dal corpo della notifica).
- **v8 (2026-07-24, DEPLOYATA): "ensure context".** (a) **Linter di aderenza** DETERMINISTICO post-bozza (zero AI): numeri/prezzi/date/URL della bozza verificati contro il corpus dei fatti consentiti (thread + BLOCCO DATI + caso + fonti + standard/tono + istruzioni); i mancanti in `non_grounded[]` per opzione e su refine (placeholder [DA VERIFICARE] esclusi; normalizza 154,90==154.9 e 10/07==2026-07-10; mai bloccante). (b) **Contratto di contesto**: `context` ritorna `gaps[]` per categoria (ordine non trovato / tracking assente / prodotto non identificato). (c) v7 aveva gia' portato: motore Claude|Gemini (`anthropic_api_key`+`cs_ai_model` in app_flags, fallback Gemini) + istruzioni team `cs_ai_istruzioni` iniettate nel system.
- **v5 (2026-07-24, DEPLOYATA): audit + motore dei verdetti.** (a) Fix audit: `dry_data` spostata in JWT_ACTIONS (era PIN pubblico -> esponeva PII; smoke: PIN -> 401 `non autenticato`); guard cross-cliente in `lookupOrder` (ordine citato nel testo deve appartenere all'email del cliente). (b) **Azione `case_data`** (JWT, ZERO Gemini): verdetto RESO (delivered_at confermata-dalla-collega o shopify-approssimata -> entro/fuori/sconosciuto vs finestra `app_flags.cs_reso_finestra_giorni`, migr 0076; `difetto_sospetto` regex -> mai rifiuto automatico) + caso INDIRIZZO (fulfillment assente=correggibile / presente=verificare_tracking, MAI "in transito" secco / delivered=consegnato; guard ordine-verificato). (c) `draft` accetta `delivered_at` e inietta il CASO nel prompt (vincolante: l'AI scrive dentro il caso, non decide). Il pannello cs-case della UI si e' acceso col deploy.
- **v4 (2026-07-23): esperienza di risposta (feedback owner).** (a) Azione **`context` (JWT)**: assembla il contesto SENZA Gemini (link ordine admin Shopify + storico acquisti cliente + fonti); la UI la chiama all'apertura del thread (nessuna spesa AI). `order_admin_url` = `https://admin.shopify.com/store/amimi-10000/orders/<id numerico>` (l'id si prende via Admin API cercando l'ordine per NOME, come il tracking); `storia` = ordini Shopify per email cliente (n_ordini, totale, prima/ultima, recenti). (b) **`draft`** ora ritorna **3 opzioni** `{tono: breve|calda|formale, testo, da_verificare}` in UNA sola chiamata Gemini (JSON mode); retro-compat: ritorna anche `draft`=opzione[0] (deploy senza finestra di rottura). Parse difensivo: JSON malformato -> fallback a 1 bozza in testo semplice, mai errore secco. (c) Azione **`refine` (JWT)**: riscrive una bozza data applicando un'istruzione della collega ("piu' formale", "aggiungi X"), sempre vincolata al BLOCCO DATI; `cs_events azione='refine'`. `dry_data` (PIN) ora ritorna anche context. **Verificato live (23-07):** `dry_data`/context su conv reale con ordine -> `order_admin_url` corretto (id 7952399794503) + storico 2 ordini/273,90 EUR. Le 3 opzioni + refine deployate ma **il test AI attende il reset quota Gemini** (429 in giornata). Frontend: 3 card opzione (scegli -> textarea editabile), input "chiedi una modifica", link ordine + storico in testata.
- **v3 (2026-07-23): libreria risposte + guardrail escalation.** (a) Seed `cs_faq` con 12 `risposta_standard` IT/EN (migr 0069, spec Cowork PARTE A) coi valori operativi confermati dall'owner (reso 14gg, corriere TWS, codice PERTE, ritiro Via Plinio 43 provvisorio); `faqTono` le inietta filtrando `categoria === conv.categoria` -> le `categoria` DEVONO essere le 13 stringhe ESATTE del classificatore (senza emoji), verificato via `dry_data` (A1 iniettata su una conv Spedizione). (b) Prompt `draft` esteso coi criteri PARTE B3: casi da NON chiudere da sola (difetto/garanzia -> mai negare il reso citando i 14gg, garanzia legale 24 mesi; disputa/chargeback/banca; reclamo/rivenditore/B2B/preventivo -> escalation), + "dai del lei se il cliente e' arrabbiato". Deploy verificato (dry_data ok post-deploy). La qualita' delle bozze (7/10) resta da misurare a quota Gemini ripristinata (summary/draft ancora in errore quota il 23-07).

## loyalty-proxy (v5) - punti fedelta' + stato di Mimi via App Proxy (identita' Shopify)

- **Scopo:** slice loyalty. Un cliente **loggato su Shopify** guadagna punti e ha uno stato persistente del personaggio **Mimi** (nanna, coccola, capo indossato, guardaroba dei suoi acquisti), senza secondo login. Sottosistema NON-core, ADDITIVO, gated.
- **Auth (HMAC App Proxy, NON JWT):** `verify_jwt=false`. Shopify firma le richieste proxate col **client secret** dell'app; la edge ricostruisce l'HMAC-SHA256 dei query param (ordinati per chiave, coppie `key=value` concatenate SENZA separatore, esclusa `signature`), in hex, confronto timing-safe. Firma assente/errata -> 401, nessuna scrittura. Distinto dall'HMAC base64 dei webhook Shopify.
- **Identita':** SOLO `logged_in_customer_id` FIRMATO dai params, MAI dal body. Vuoto (cliente non loggato) -> `{state:'login_required'}`, nessuna scrittura.
- **Segreto:** `app_flags.shopify_app_proxy_secret` (convenzione codebase, come qromo_webhook_secret/gemini_api_key), fallback env `SHOPIFY_APP_PROXY_SECRET`. Assente -> `{state:'needs_secret'}` 200 (deployabile/testabile PRIMA della config Shopify, non crash). L'owner/Cowork lo inserisce DOPO aver configurato l'app (canale sicuro, mai in repo/chat).
- **Azioni** (dal path App Proxy `/apps/premia/<azione>` o `?action=`): `balance` (GET) -> `{points}` del cliente (0 se assente); `add` (POST `{score}`) -> anti-abuso PRIMA di scrivere, poi `loyalty_points.points += clamp` + insert `loyalty_events`.
- **Anti-abuso:** score clampato a `CAP_PER_GAME=100`; cap giornaliero `DAILY_CAP=200` punti/cliente (somma dei delta di oggi UTC); rate-limit `RATE_LIMIT_SEC=30` (1 partita/30s). Oltre soglia -> `{capped:true, reason:'rate'|'daily'|'zero'}` SENZA sforare ne' scrivere. `capped` nella risposta = accreditato MENO del richiesto (clamp-partita o cap giornaliero).
- **Scritture:** SOLO `loyalty_points` + `loyalty_events` col service_role (RLS nega ogni accesso client, SCHEMA §7). NON passa da write-api: come `cs_*`, la edge protetta E' il canale di scrittura controllato (spirito Regola 13; non tocca CE/stock/inventario/Qromo).
- **Verifica (2026-07-23, criteri brief 2-7):** firma assente/manomessa -> 401 no-write; empty customer -> login_required; add normale (50), clamp 150->100, rate-limit, cap giornaliero tutti corretti; `loyalty_events` coerente (0 scritture sui path cappati, persistenza verificata); anon/authenticated 0 privilegi SELECT/INSERT (RLS+REVOKE); needs_secret senza segreto; 0 segreti nel bundle. Testato con secret di prova TEMPORANEO (migr `tmp_loyalty_smoke_0068` + `_cleanup`, tabelle poi vuote, edge tornata a needs_secret). **Criterio 1 (firma valida end-to-end) = con la config Shopify reale, owner/Cowork.**
- **URL edge (target dell'App Proxy):** `https://imszbjeyplaiovylhkgl.supabase.co/functions/v1/loyalty-proxy`. Config lato Shopify (app + App Proxy prefix `apps`, subpath `premia`, URL=questo) + client secret = Cowork/owner, fuori dal repo.
- **Config Shopify CHIUSA (2026-07-24, Cowork co-pilota con l'owner; dettagli in `Cowork12/docs/_CHANGELOG.md` 24-07).** App **"Amimi Premia"** creata nella **Dev Dashboard** (versione `amimi-premia-2` attiva, embedded false), Proxy dell'app prefix `apps` / subpath `premia` -> URL edge qui sopra; app installata su `amimi-10000` con **zero scope Admin API**. Client secret inserito in `app_flags.shopify_app_proxy_secret` via canale sicuro (mai in repo/chat/log). **Gotcha duraturo:** le custom app "Develop apps" dell'admin **non offrono piu' l'App Proxy**, l'admin rimanda alla Dev Dashboard (dev.shopify.com).
- **CRITERIO 1 CHIUSO (firma valida end-to-end).** `amimi.it/apps/premia/balance` -> `needs_secret` prima della config, `{points:0}` dopo con cliente loggato (HMAC valido); `POST /apps/premia/add` `score 5` -> `{points:5,added:5,capped:false}`; righe reali in `loyalty_points`/`loyalty_events` (customer `10147859530055`, source `game_click`). Clicker montato nella pagina Shopify **`premia-gioco`** (id 165375672647), **pubblicata** su OK owner il 24-07 (test a video passato, saldo aggiornato in pagina) ma non linkata da nessun menu; `premia-anteprima` intatta e nascosta. Con l'uso reale dell'owner cap-partita e rate-limit hanno retto.
- **Stato live (verificato 2026-07-26):** edge v4 ACTIVE, secret presente, `loyalty_points` 1 riga (customer `10147859530055`, **222 punti**, ultimo aggiornamento 24-07 20:59 UTC), `loyalty_events` 6 righe. Sono punti di **test/gioco reale**: azzerabili col service role se l'owner lo chiede (decisione owner, non fatto).
- **RESTA (slice v1):** nulla lato Claude Code. Fase dopo (non questa slice): sconto personale reale via Admin API.

### v5 (2026-07-26, DEPLOYATA) - Mimi / Profilo Premia Fase 1+2

Brief `2026-07-24_CLAUDE_CODE_BRIEF_mimi_profilo_fase1.md`, decisioni owner 24-07. Migration **0078** (`mimi_state`). Backend + schema soltanto: **le pagine Shopify le monta Cowork**.

- **Tabella `mimi_state`** (migr 0078): `shopify_customer_id` PK, `nanna`, `last_coccola`, `last_memory`, `worn`, `updated_at`. Stessa postura di `loyalty_*` (migr 0068): **RLS ON senza policy + `revoke all from anon, authenticated`**. Scrive solo la edge col service_role.
- **Azioni nuove** (tutte dietro la stessa firma HMAC + `logged_in_customer_id` firmato):
  - `state` (GET): un giro solo per disegnare il profilo -> `{points, nanna, coccola_disponibile, memory_disponibile, worn, wardrobe[]}`. Ogni voce di `wardrobe`: `{codice, titolo, img, data, canale:'online'}`.
  - `coccola` (POST): +5, **max 1/giorno**. Gia' fatta oggi -> `{done_today:true, points}` **senza scrivere**.
  - `memory_win` (POST): +5, max 1/giorno, `source='game_memory'`. E' il gioco pubblico al posto del clicker. La vittoria la dichiara il client: accettato dall'owner perche' il premio e' **fisso, piccolo e 1/giorno** (il client chiede, la edge decide l'importo).
  - `nanna` (POST `{value:boolean}`): persiste lo stato "Mimi nel sacchettino", cross-device. Valore non booleano -> 400.
  - `wear` (POST `{codice}`): veste Mimi **solo** con un capo realmente acquistato; altrimenti **409 `not_owned`** senza scrivere.
- **GIORNO = Europe/Rome** per le azioni nuove (`Intl.DateTimeFormat('en-CA',{timeZone:'Europe/Rome'})`, l'ora legale la gestisce Intl). **`add` resta su UTC**: e' il comportamento storico del clicker e cambiarlo avrebbe rotto la retro-compat (criterio 6). Verificato che 23:30 e 00:30 italiane cadono in due giorni diversi, sia in ora legale che solare.
- **Clicker `add` in RISERVA:** gated da `app_flags.loyalty_click_enabled` (**default `'false'`**). Spento -> `{state:'off'}`, zero scritture. Acceso -> comportamento **identico** a prima (nulla e' stato rimosso, decisione owner punto 4).
- **coccola/memory NON passano dal `DAILY_CAP`** del clicker: il loro cancello (1/giorno, +5) e' piu' stretto. I loro delta restano pero' su `loyalty_events`, quindi **consumano** il cap giornaliero del clicker se l'owner lo riaccende. Scelta conservativa: mai piu' punti del previsto.
- **Guardaroba (la parte delicata).** Calcolato **al volo a ogni `state`** (pochi ordini per cliente, nessuno snapshot da invalidare). **`shopify_orders` NON ha il customer_id** e l'App Proxy da' solo l'id numerico: l'unico ponte id -> ordini e' l'Admin API `orders.json?customer_id=...&status=any` (serve **solo `read_orders`**, gia' in uso da shopify-sync -> **nessuno scope nuovo**, e l'app "Amimi Premia" resta a zero scope). Dai nomi ordine (`shopify_line_items.order_id` tiene il **NOME** `#1582`, non l'id numerico) si prendono `codice` e poi `shopify_stock` per titolo/immagine. Un capo per codice, il piu' recente. Righe non risolte escluse. Si parte sempre dall'id firmato: il filtro non si allarga mai ad altri clienti. Acquisti **offline Qromo fuori scope** (vendite anonime).
- **Verifica (2026-07-26, 8 criteri del brief).** Firmata con un **clone usa-e-getta** (`loyalty-proxy-smoke`, un solo carattere di differenza: leggeva `shopify_app_proxy_secret_smoke`) per **non toccare il segreto di produzione** ne' rischiare buchi sulla pagina `premia-gioco` gia' pubblica. Esiti: `state` completo; **guardaroba combaciante con l'oracolo Shopify** (cliente reale con 1 ordine e 2 articoli -> esattamente quei 2 codici con immagine; altro cliente -> 0 capi, nessuna contaminazione); coccola/memory +5 e seconda chiamata `done_today` **con 0 eventi scritti**; nanna true/false riletta da `state`, valore non booleano 400; `wear` 409 su capo non posseduto e su codice inventato, 200 su capo posseduto; `add` con flag off -> `{state:'off'}` 0 scritture; **401 su tutte le azioni** senza firma, con firma manomessa e con firma di un altro segreto; `anon`/`authenticated` -> `permission denied` in SELECT e INSERT su `mimi_state`. **Stato ripristinato a fine test** (guardia sul saldo): punti 232 -> **222** (pre-test), 2 eventi di prova cancellati, `mimi_state` **0 righe**, flag smoke rimosso, segreto di produzione **mai letto e intatto** (len 38). Il clone e' stato **ritirato a stub 410** (il connettore Supabase non espone la cancellazione di una edge function: resta il guscio, come `etl-load`).
- **HANDBACK a Cowork** (come da brief): nomi azioni e shape JSON sono quelli qui sopra. Da fare lato Cowork: pagine profilo V6 / stanza V7 / memory sul canale `/apps/premia/...`, **copy allineato a +5** (i prototipi dicono +10), test end-to-end da cliente loggato. Owner: OK finale e decisione se rinascondere `premia-gioco`.

## etl-load (v4) - RITIRATA

- Stub che risponde 410 Gone: era il loader one-off del re-seed 2026-07-01. Nessuna azione. Candidata alla cancellazione.

## Flag e config di riferimento

- `app_config`: `pin_hash`, `shopify_token`, `iva_rate` (0.22), `parity_tolerance_cents`, `ai_enabled` (gate dell'assistente AI in-app, default false, go-live owner, dal 2026-07-22; esposto ad anon come booleano via `v_ops_flags`, migr 0057).
- `app_flags`: `shopify_write_enabled`, `shopify_autopush_enabled`, `shopify_expose_buffer` (oggi 0), `shopify_hold_raises`, `shopify_autoenable_tracking` (default off: riaccende il tracking magazzino su item fisici untracked prima del set, mai gift card, dal v11), `shopify_location_id`, `qromo_webhook_secret`, `qromo_webhook_token`, `gemini_api_key`, `mcp_token`. Entrambe le tabelle sono service-role only (lockdown migr 0026); i flag si cambiano SOLO con decisione owner (Regola 15/16).
- Chiavi `cs_*` (tool assistenza, dal 2026-07-20, Fase 0): `cs_enabled` ('false' = sync spento), `cs_last_history_id` (cursore Gmail, vuoto), `cs_gmail_sa_key` (chiave JSON service account, vuota finche' l'owner non la inserisce via canale sicuro), `cs_ntfy_topic_benny` / `cs_ntfy_topic_ginevra` / `cs_ntfy_topic_ale` (topic ntfy con suffisso random generato direttamente nel DB: i valori NON sono mai transitati in repo, doc o chat e sono da trattare come segreti). Inserite via SQL one-off, NON via migrazione (repo pubblico).

## activity-digest (v2) - riepilogo Gemini delle ultime attivita' (redesign §6)

- **Scopo:** riassume in italiano (2-3 frasi) le ultime ~25 righe di `change_log` per la card "Attivita' recente" della pagina Salute & Movimenti (redesign). SOLA LETTURA, nessuna scrittura.
- **Auth:** PIN (come ask-data). `verify_jwt=false`.
- **Gemini:** key da `app_flags.gemini_api_key` (server-only). Modello `gemini-flash-lite-latest` (NIENTE flash "thinking" che divora il budget di output e restituisce artefatti di reasoning; MAI `thinkingConfig` -> 400). `maxOutputTokens 320`, retry su 503/429 transitori. `needs_key` se la chiave manca.
- **Client:** `fetchActivityDigest(pin)` -> card `ds-aisum` in `ActivityFeed.tsx`, generazione ON-DEMAND (bottone "Genera riepilogo") + cache in `localStorage` (`amimi_actdigest`) + "Rigenera"; NON si auto-genera a ogni visita (risparmio quota). 429/quota gestito con messaggio "Quota AI esaurita per oggi".
- **Verifica (2026-07-23):** deployata ACTIVE v2; PIN errato -> 401; v1 ha prodotto un riassunto reale; con quota Gemini free esaurita restituisce 429 (gestito lato client, non e' un bug). Nessuna scrittura (change_log/health_log invariati).
